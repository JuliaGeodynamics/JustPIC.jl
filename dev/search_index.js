var documenterSearchIndex = {"docs":
[{"category":"section","location":"CellArrays/#Working-with-CellArrays","page":"CellArrays","text":"CellArrays are the storage primitive behind JustPIC particle containers. They represent a grid where every logical grid cell stores a small fixed-size payload, for example the particle slots belonging to that cell.","title":"Working with CellArrays"},{"category":"section","location":"CellArrays/#Instantiating-a-CellArray","page":"CellArrays","text":"cell_array(backend, value, ncells, ni) allocates a CellArray over an ni-shaped grid, giving every cell an ncells-shaped payload initialized to value. The payload is what holds per-particle data — coordinates, temperature, phase labels — for the particles belonging to that grid cell.\n\njulia> using JustPIC\n\njulia> import CellArraysIndexing as CAI\n\njulia> ni = (2, 2)      # grid cells\n(2, 2)\n\njulia> ncells = (2,)    # payload slots per cell\n(2,)\n\njulia> CA = cell_array(JustPIC.CPU, 20.0, ncells, ni)\n2×2 CellArrays.CPUCellArray{StaticArraysCore.SVector{2, Float64}, 2, 1, Float64}:\n [20.0, 20.0]  [20.0, 20.0]\n [20.0, 20.0]  [20.0, 20.0]","title":"Instantiating a CellArray"},{"category":"section","location":"CellArrays/#Indexing-a-CellArray","page":"CellArrays","text":"Indexing by grid cell returns the whole payload stored in that cell. This is convenient for inspection, but it materializes a StaticArray value:\n\njulia> CA[1, 1]\n2-element StaticArraysCore.SVector{2, Float64} with indices SOneTo(2):\n 20.0\n 20.0\n\nIt is however useful to read and mutate the data of the CellArray object directly, without instantiating a StaticArray. For this purpose, CellArraysIndexing provides @index to directly read and mutate the individual elements of the cell.\n\nFor example, to read a single element of CA:\n\njulia> CAI.@index CA[2, 1, 1]\n20.0\n\nHere the first index selects the payload entry and the remaining indices select the grid cell. Mutation uses the same syntax:\n\njulia> CAI.@index CA[2, 1, 1] = 0.0;\n\njulia> CA\n2×2 CellArrays.CPUCellArray{StaticArraysCore.SVector{2, Float64}, 2, 1, Float64}:\n [20.0, 0.0]   [20.0, 20.0]\n [20.0, 20.0]  [20.0, 20.0]\n\n@cell is the companion macro for reading or writing an entire cell payload:\n\njulia> @cell CA[1, 1]\n2-element StaticArraysCore.SVector{2, Float64} with indices SOneTo(2):\n 20.0\n  0.0\n\njulia> @cell CA[1, 1] = @cell(CA[1, 1]) .+ 1;\n\njulia> CA\n2×2 CellArrays.CPUCellArray{StaticArraysCore.SVector{2, Float64}, 2, 1, Float64}:\n [21.0, 1.0]   [20.0, 20.0]\n [20.0, 20.0]  [20.0, 20.0]","title":"Indexing a CellArray"},{"category":"section","location":"CellArrays/#Backend-and-layout-notes","page":"CellArrays","text":"cell_array dispatches on the KernelAbstractions backend, so the same call allocates a device-resident CellArray when a vendor extension is loaded. The backing layout differs between them: the CPU path uses a block length of 1, while GPU backends use a structure-of-arrays layout. Always go through CAI.@index, @cell, cellaxes and cellnum rather than indexing CA.data directly, so that code stays portable across backends.","title":"Backend and layout notes"},{"category":"section","location":"CellArrays/#API","page":"CellArrays","text":"","title":"API"},{"category":"function","location":"CellArrays/#JustPIC.cell_array","page":"CellArrays","text":"cell_array(backend, x, ncells::NTuple, ni::NTuple)\ncell_array(x, ncells::NTuple, ni::NTuple)\n\nAllocate a CellArray on backend (a KernelAbstractions backend type such as CPU), with ncells entries per grid cell over a grid of size ni, and fill every entry with x.\n\nThe backend form is the preferred allocation path for particle storage and phase-ratio arrays. The backend-less form allocates on CPU.\n\nExamples\n\nindex = cell_array(CPU, false, (24,), (64, 64))\nfield = cell_array(CPU, 0.0, (3,), (64, 64))\n\n\n\n\n\n","title":"JustPIC.cell_array"},{"category":"function","location":"CellArrays/#JustPIC.CA","page":"CellArrays","text":"CA(backend, dims; eltype = Float64)\n\nAllocate an uninitialized CellArray of size dims on backend. Extended for the GPU backends by the package extensions.\n\n\n\n\n\n","title":"JustPIC.CA"},{"category":"function","location":"CellArrays/#JustPIC.cellaxes","page":"CellArrays","text":"cellaxes(A)\n\nReturn the one-based axes used to iterate over the entries inside each CellArray cell.\n\nThis is the preferred helper for loops over particle slots because it works for both scalar and multi-entry cell storage.\n\n\n\n\n\n","title":"JustPIC.cellaxes"},{"category":"function","location":"CellArrays/#JustPIC.cellnum","page":"CellArrays","text":"cellnum(A::CellArray)\n\nReturn the number of storage slots inside each logical cell of A.\n\nFor particle containers this is the number of particle slots reserved per grid cell, including inactive slots.\n\n\n\n\n\n","title":"JustPIC.cellnum"},{"category":"function","location":"CellArrays/#JustPIC.update_cell_halo!","page":"CellArrays","text":"update_cell_halo!(x::CellArray...)\n\nSynchronize the overlapping MPI halo of one or more CellArrays in place.\n\nThis is the CellArray companion to ImplicitGlobalGrid.update_halo! and is typically used after particle coordinates or per-particle fields have changed on each rank.\n\nArguments\n\nx: one or more CellArrays with the same logical grid layout.\n\nNotes\n\nEvery provided CellArray is updated; this is convenient for particles.coords, particles.index, and particle field arrays returned by init_cell_arrays.\nFor MPI particle advection, halo exchange is usually required before move_particles! so that particles that crossed a rank boundary are visible to the neighboring rank.\nWith periodic boundary conditions, update_cell_halo! exchanges the overlap across the periodic domain boundaries as configured in init_global_grid.\nIf particles are reinjected with inject_particles!, refresh the halos again before reconstructing grid fields with particle2grid!.\n\nExample\n\nadvection!(particles, RungeKutta2(), V, dt)\nupdate_cell_halo!(particles.coords...)\nupdate_cell_halo!(particle_args...)\nupdate_cell_halo!(particles.index)\nmove_particles!(particles, particle_args)\ninject_particles!(particles, particle_args)\nparticle2grid!(T, pT, particles)\n\n\n\n\n\n","title":"JustPIC.update_cell_halo!"},{"category":"section","location":"API/#Public-API","page":"Public API","text":"The reference is organized by workflow. Each manual page documents the symbols belonging to its topic; this page collects the remaining backend and grid helpers and provides a searchable index of everything.","title":"Public API"},{"category":"section","location":"API/#Where-to-find-each-group","page":"Public API","text":"Topic Page\nParticle containers, initialization, advection, maintenance, phase ratios, subgrid diffusion Particles\nCell storage, allocation and halo exchange CellArrays\nGrid/particle and centroid transfers Interpolations\nVelocity reconstruction schemes Velocity interpolation\nMarker-chain surface tracking Marker chain\nMarker-surface (3D free surface) tracking Marker surface\nCheckpointing and restart I/O","title":"Where to find each group"},{"category":"section","location":"API/#Backends","page":"Public API","text":"JustPIC dispatches on KernelAbstractions backend types directly — CPU for the CPU path and the vendor backends CUDA.CUDABackend, AMDGPU.ROCBackend, Metal.MetalBackend, introduced by the respective package extensions. Load the vendor package to activate its extension; JustPIC defines no backend tags of its own.\n\nTA(backend) selects the plain array type for a backend, and launch! runs a KernelAbstractions kernel on it, synchronizing before returning.","title":"Backends"},{"category":"section","location":"API/#Grid-helpers","page":"Public API","text":"","title":"Grid helpers"},{"category":"section","location":"API/#Index","page":"Public API","text":"","title":"Index"},{"category":"function","location":"API/#JustPIC.TA","page":"Public API","text":"TA()\nTA(backend)\n\nReturn the plain array type associated with backend (a KernelAbstractions backend type such as CPU).\n\nFor CPU this is Array. Loading CUDA.jl / AMDGPU.jl / Metal.jl extends this for CUDA.CUDABackend, AMDGPU.ROCBackend, and Metal.MetalBackend, respectively.\n\n\n\n\n\n","title":"JustPIC.TA"},{"category":"function","location":"API/#JustPIC.launch!","page":"Public API","text":"launch!(backend, kernel!, ndrange, args...)\n\nInstantiate and run KernelAbstractions kernel! on backend over ndrange, then block until it completes.\n\nThe trailing synchronize keeps the package's historical synchronous launch semantics: host reads, MPI halo exchanges and injection/cleanup all assume the previous kernel has finished. A later optimization pass may drop per-launch synchronization in favor of synchronizing only before host access.\n\n\n\n\n\n","title":"JustPIC.launch!"},{"category":"function","location":"API/#JustPIC.add_periodic_ghost_nodes","page":"Public API","text":"add_periodic_ghost_nodes(x::AbstractVector)\n\nExtend a 1D periodic grid with one ghost node on each side.\n\nThe added coordinates preserve the spacing of the last and first physical cells, respectively, which makes this helper work for both uniform and refined grids.\n\nExample\n\nxv = [0.0, 0.25, 0.5, 0.75, 1.0]\nxv_periodic = add_periodic_ghost_nodes(xv)\n\n\n\n\n\n","title":"JustPIC.add_periodic_ghost_nodes"},{"category":"section","location":"velocity_interpolation/#Velocity-interpolation","page":"Velocity interpolation","text":"Particle and marker advection needs velocities at particle positions, while geodynamic velocity fields are usually stored on staggered grids. JustPIC offers three interpolation paths for this step.","title":"Velocity interpolation"},{"category":"section","location":"velocity_interpolation/#Linear","page":"Velocity interpolation","text":"The default advection! path uses bilinear interpolation in 2D and trilinear interpolation in 3D. This is the cheapest and most general option, and it is the right default for most examples and tests.","title":"Linear"},{"category":"section","location":"velocity_interpolation/#LinP","page":"Velocity interpolation","text":"advection_LinP! uses the linear-plus-pressure interpolation described by Pusok et al. 2017. The velocity at the m-th particle is given by\n\nu_m = A u_L + (1-A) u_P\n\nwhere u_L is the bi- or trilinear interpolation from velocity nodes to the particle, u_P is the interpolation from pressure nodes to the particle, and A = 23 is an empirical coefficient.\n\n(Image: LinP velocity interpolation stencil)","title":"LinP"},{"category":"section","location":"velocity_interpolation/#Modified-Quadratic-Spline","page":"Velocity interpolation","text":"advection_MQS! uses the modified quadratic spline interpolation from Gerya et al. 2021. The scheme is designed so that velocity derivatives can be reconstructed from pressure-node locations where they are constrained by the continuity equation. Near boundaries, where the required stencil is unavailable, the implementation falls back to the linear interpolation path.\n\nExample for the u_x component in 2D:\n\n(Image: Modified quadratic spline stencil for the `x` velocity component in 2D)\n\nStep 1: compute the normalized distances between the particle and the velocity node (i j) at the lower-left corner of the interpolation cell:\n\nt_x = fracx_m - x_iDelta x\n\nt_y = fracy_m - y_jDelta y\n\nStep 2: lerp along x on the bottom and top edges of the cell:\n\nu_m^textbot = (1 - t_x) u_ij + t_x u_i+1j\n\nu_m^texttop = (1 - t_x) u_ij+1 + t_x u_i+1j+1\n\nStep 3: add the quadratic correction, a second difference along x over the three nodes closest to the particle. Which triplet that is depends on which half of the cell the particle sits in — the figure above shows the t_x  12 case:\n\nu_m^textbot mathrel+= frac12 left(t_x - frac12right)^2\nbegincases\nu_i-1j - 2 u_ij + u_i+1j  t_x  12 \nu_ij - 2 u_i+1j + u_i+2j  t_x geq 12\nendcases\n\nu_m^texttop mathrel+= frac12 left(t_x - frac12right)^2\nbegincases\nu_i-1j+1 - 2 u_ij+1 + u_i+1j+1  t_x  12 \nu_ij+1 - 2 u_i+1j+1 + u_i+2j+1  t_x geq 12\nendcases\n\nStep 4: lerp the corrected values along y:\n\nu_m = (1-t_y) u_m^textbot + t_y u_m^texttop\n\nThe u_y component uses the same construction with the roles of the two directions exchanged.","title":"Modified Quadratic Spline"},{"category":"section","location":"velocity_interpolation/#Choosing-a-Scheme","page":"Velocity interpolation","text":"Use advection! for the default linear interpolation.\nUse advection_LinP! when matching the LinP reconstruction from the cited PIC literature.\nUse advection_MQS! when the modified quadratic spline stencil is desired and the grid has enough interior support.\nUse semilagrangian_advection!, semilagrangian_advection_LinP!, or semilagrangian_advection_MQS! for grid-field backtracking with the corresponding velocity reconstruction.","title":"Choosing a Scheme"},{"category":"section","location":"donut_advection_periodic/#Periodic-donut-advection-in-2D","page":"Periodic donut advection in 2D","text":"The script scripts/donut_advection_periodic.jl shows how to advect a compact scalar structure through a rectangular domain with periodic boundary conditions in the horizontal direction.\n\nThe setup uses a constant analytical velocity field:\n\nvx_stream(x, y) = 1.0\nvy_stream(x, y) = 0.0\n\nParticles are initialized on the staggered velocity grid in the usual way:\n\ngrid_vx = xv, expand_range(yc)\ngrid_vy = expand_range(xc), yv\nparticles = init_particles(\n    backend, nxcell, max_xcell, min_xcell, grid_vx, grid_vy\n)\n\nThe scalar field is initialized as an annulus, which makes it easy to inspect how well the particle-to-grid transfer preserves a sharp structure during transport:\n\n@inline incircles(x, y, xc, yc, r1, r2) =\n    r1^2 ≤ (x - xc)^2 + (y - yc)^2 ≤ r2^2\n\nT = TA(backend)([\n    incircles(x, y, xc, yc, r1, r2) * 1.0\n    for x in particles.xvi[1], y in particles.xvi[2]\n])\n\nAfter interpolating T to the particle field with grid2particle!, the time loop advances particles with advection!, restores cell sorting with move_particles!, injects particles where needed, and reconstructs the field on the grid:\n\nfor _ in 1:frame_stride\n    advection!(particles, RungeKutta2(), V, dt; periodic_1 = true)\n    move_particles!(particles, particle_args; periodic_1 = true, periodic_2 = false)\n    inject_particles!(particles, (pT,))\n    particle2grid!(T, pT, particles)\nend\n\nThe key detail is enabling periodic_1 for both advection! and move_particles!. This wraps particles that leave the domain in the first coordinate direction back to the opposite side, while the second direction remains non-periodic.\n\nThe script also uses GLMakie to record a GIF of the evolving annulus, making it a convenient example for checking periodic transport and visualization together.\n\nIn practice, this example is most useful when you want to verify three things at once:\n\nparticles can leave and re-enter the domain cleanly in the periodic direction,\ninject_particles! maintains particle density during transport,\nthe reconstructed annulus stays coherent after repeated particle2grid! transfers.","title":"Periodic donut advection in 2D"},{"category":"section","location":"IO/#Checkpointing","page":"I/O","text":"","title":"Checkpointing"},{"category":"section","location":"IO/#Writing-checkpoint-files","page":"I/O","text":"Long-running simulations commonly write checkpoint files so that runs can be restarted from a recent saved state. In JustPIC, checkpoints are written in JLD2 format.\n\nAt the lowest level, you can serialize arrays manually:\n\njldsave(\n    \"my_file.jld2\";\n    particles     = Array(particles),\n    phases        = Array(phases),\n    phase_ratios  = Array(phase_ratios),\n    particle_args = Array.(particle_args),\n)\n\nThis saves particle information to my_file.jld2, ready to be reloaded later.\n\nIf file size matters more than exact restart reproducibility, you can downcast to Float32 before writing:\n\njldsave(\n    \"my_file.jld2\";\n    particles     = Array(Float32, particles),\n    phases        = Array(Float32, phases),\n    phase_ratios  = Array(Float32, phase_ratios),\n    particle_args = Array.(Float32, particle_args),\n)\n\nFor routine use, prefer the built-in helper:\n\ncheckpointing_particles(\n    dst,\n    particles;\n    phases = nothing,\n    phase_ratios = nothing,\n    chain = nothing,\n    t = nothing,\n    dt = nothing,\n    particle_args = nothing,\n    kwargs...,\n)\n\nOn multiple MPI ranks, pass the rank id to get rank-local filenames:\n\ncheckpointing_particles(\n    dst,\n    particles,\n    me;\n    phases = nothing,\n    phase_ratios = nothing,\n    chain = nothing,\n    t = nothing,\n    dt = nothing,\n    particle_args = nothing,\n    kwargs...,\n)\n\nAny additional keyword arguments are written into the checkpoint as extra fields.","title":"Writing checkpoint files"},{"category":"section","location":"IO/#Loading-a-checkpoint-file","page":"I/O","text":"To restart a simulation, load the file and cast the stored arrays back to the active backend:\n\ndata          = load(\"my_file.jld2\")\nparticles     = TA(backend)(Float64, data[\"particles\"])\nphases        = TA(backend)(Float64, data[\"phases\"])\nphase_ratios  = TA(backend)(Float64, data[\"phase_ratios\"])\nparticle_args = TA(backend).(Float64, data[\"particle_args\"])\n\nTA(backend) selects the backend-appropriate array type, so the same checkpoint can be restored onto CPU or accelerator arrays.","title":"Loading a checkpoint file"},{"category":"section","location":"IO/#API","page":"I/O","text":"","title":"API"},{"category":"function","location":"IO/#JustPIC.checkpointing_particles","page":"I/O","text":"checkpointing_particles(dst, particles; phases=nothing, phase_ratios=nothing, chain=nothing, t=nothing, dt=nothing, particle_args=nothing)\ncheckpointing_particles(dst, particles, me; phases=nothing, phase_ratios=nothing, chain=nothing, t=nothing, dt=nothing, particle_args=nothing)\n\nWrite particle state and optional companion data to a JLD2 checkpoint.\n\nBy default the file is saved as particles_checkpoint.jld2 in dst. Additional keyword arguments are serialized into the checkpoint after being converted to plain Julia arrays where needed.\n\nCommon keywords\n\nphases: per-particle phase labels.\nphase_ratios: PhaseRatios container to checkpoint.\nchain: marker-chain state.\nt: simulation time.\ndt: timestep size.\nparticle_args: tuple of extra particle-carried fields.\n\nNotes\n\nArrays are converted to plain Julia arrays before serialization so the checkpoint can be reloaded independently of the active backend.\nPassing me writes rank-local files named after the zero-based MPI rank: particles_checkpoint0000.jld2, particles_checkpoint0001.jld2, and so on.\n\n\n\n\n\n","title":"JustPIC.checkpointing_particles"},{"category":"section","location":"interpolations/#Interpolations","page":"Interpolations","text":"The interpolation routines in JustPIC target rectangular 2D and 3D grids and support both uniform and coordinate-vector refined grids.","title":"Interpolations"},{"category":"section","location":"interpolations/#Grid-to-particle","page":"Interpolations","text":"Information is transferred from nodal or staggered grids to particle-carried fields with linear interpolation. The one-dimensional linear interpolation kernel is\n\nv_textp = t v_0  + (1 -t) v_1\n\nwhere the t, v_0, and v_1 are graphically described below.\n\n(Image: Linear interpolation between the nodal values `v0` and `v1`)\n\nNumerically, it is more appropriately implemented as a double fma as it is slightly more accurate than a naive implementation:\n\nv_p = fma(t, v1, fma(-t, v0, v0))\n\nBi- and tri-linear interpolation over a rectangular or cubic cells is thus nothing else than a chain of lerp kernels along the different dimensions of the cell. For example, the bilinear interpolation requires two lerps along the left and right sides of the cell, followed by a lerp on the horizontal direction; and trilinear interpolation requires two bilinear kernels and one lerp.\n\nN-linear interpolation is implemented recursively to keep the code dimension agnostic while staying friendly to compiler specialization.\n\nWe can interpolate an arbitrary field F onto particles with grid2particle!:\n\nusing JustPIC\n# define model domain\nnxcell, max_xcell, min_xcell = 24, 30, 12\nn = 129\nLx  = Ly = 1.0\nxvi = xv, yv = range(0, Lx, length=n), range(0, Ly, length=n)\ndx, dy = step(xv), step(yv)\nxc = range(dx / 2, Lx - dx / 2, length=n - 1)\nyc = range(dy / 2, Ly - dy / 2, length=n - 1)\ngrid_vx = xv, range(first(yc) - dy, step=dy, length=n + 1)\ngrid_vy = range(first(xc) - dx, step=dx, length=n + 1), yv\nparticles = init_particles(\n    JustPIC.CPU, nxcell, max_xcell, min_xcell, grid_vx, grid_vy\n)\n# field F at the grid\nF = [y for x in particles.xvi[1], y in particles.xvi[2]]\n# instantiate empty `CellArray`\nFp, = init_cell_arrays(particles, Val(1));\n# interpolate F onto Fp\ngrid2particle!(Fp, F, particles);\n\nparticles.xvi includes one ghost node on each side, so the compact call above expects F to use that same layout. For a field stored only on physical nodes, disable the shift in each unpadded direction:\n\nF_physical = [y for x in xv, y in yv]\ngrid2particle!(Fp, F_physical, particles; ghost_1=false, ghost_2=false)","title":"Grid to particle"},{"category":"section","location":"interpolations/#Particle-to-grid","page":"Interpolations","text":"Information on particles can be accumulated back to grid nodes with inverse distance weighting:\n\nv_ij = fracsum^N_k=1 omega_k v_ksum^n_k=1 omega_k\n\nwhere the weight is omega_i = d^-n, with d being the distance between the particle and the node, and n a integer number.\n\nOn shared-memory hardware this typically requires atomics. JustPIC avoids that by looping over grid nodes and scanning only the neighboring particle cells that can contribute to each node.\n\nThis interpolation is handled by particle2grid!:\n\njulia> particle2grid!(F, Fp, particles)\n\nThe same ghost_1, ghost_2, and ghost_3 keywords select whether each destination direction includes particle ghost nodes. They default to true.\n\nparticle2centroid! and grid2particle_flip! take the same keywords; for particle2centroid! they refer to the ghosted centroid grid particles.xci. centroid2particle! has no opt-out: particles sitting between a domain boundary and the first centroid are interpolated from the ghost centroids, so its source field must always use the particles.xci layout.\n\nRelated high-level helpers in this workflow are particle2centroid!, centroid2particle!, update_phase_ratios!, subgrid_diffusion!, and subgrid_diffusion_centroid!. The two subgrid-diffusion routines read their T_grid through grid2particle!/centroid2particle! and so expect the ghosted vertex and centroid layouts respectively, while ΔT_grid carries one ghost node per side, matching size(particles.index).","title":"Particle to grid"},{"category":"section","location":"interpolations/#API","page":"Interpolations","text":"","title":"API"},{"category":"function","location":"interpolations/#JustPIC.grid2particle!","page":"Interpolations","text":"grid2particle!(Fp, xvi, F, particles::PassiveMarkers)\n\nInterpolate a nodal field F to passive-marker values Fp, updated in place.\n\nThe vertex grid xvi must be supplied explicitly, since PassiveMarkers stores only marker coordinates and no grid metadata.\n\nArguments\n\nFp: destination marker field, or tuple of marker fields.\nxvi: vertex coordinates of the grid on which F is defined.\nF: source nodal field, or tuple of nodal fields matching Fp.\nparticles: PassiveMarkers container supplying marker coordinates.\n\n\n\n\n\n","title":"JustPIC.grid2particle!"},{"category":"function","location":"interpolations/#JustPIC.grid2particle_flip!","page":"Interpolations","text":"grid2particle_flip!(Fp, xvi, F, F0, particles; α = 0.0)\n\nUpdate particle values with a PIC/FLIP blend.\n\nα = 1 gives pure PIC, α = 0 gives pure FLIP, and intermediate values blend between the two updates.\n\nArguments\n\nFp: particle field to update in place.\nF: current grid field.\nF0: previous grid field.\nparticles: particle container.\nα: PIC fraction in the PIC/FLIP blend.\nghost_1, ghost_2, ghost_3: whether F and F0 include ghost nodes in each coordinate direction. Disable a keyword for a physical-only direction.\n\n\n\n\n\n","title":"JustPIC.grid2particle_flip!"},{"category":"function","location":"interpolations/#JustPIC.particle2grid!","page":"Interpolations","text":"particle2grid!(F, Fp, buffer, xi, particles::PassiveMarkers)\n\nInterpolate passive-marker values Fp onto the grid nodes F, overwriting F in place.\n\nBecause passive markers scatter to arbitrary nodes, weights are accumulated with atomic updates into F and buffer and normalized in a final pass; buffer must be a scratch array with the same size as F. The vertex grid xi is supplied explicitly.\n\nArguments\n\nF: destination nodal array.\nFp: marker field stored with the same layout as particles.coords.\nbuffer: scratch nodal array (same size as F) used to accumulate weights.\nxi: vertex coordinates of the target grid.\nparticles: PassiveMarkers container supplying marker coordinates.\n\n\n\n\n\n","title":"JustPIC.particle2grid!"},{"category":"function","location":"interpolations/#JustPIC.centroid2particle!","page":"Interpolations","text":"centroid2particle!(Fp, xci, F, particles)\n\nInterpolate cell-centered field values F to particle values Fp.\n\nxci contains the center coordinates of the grid carrying F. The destination Fp is mutated in place and may be either a single particle field or a tuple of particle fields.\n\nParticles lying between a domain boundary and the first centroid are interpolated from the ghost centroids, so F must always use the ghosted particles.xci layout — unlike grid2particle!, there is no opt-out.\n\n\n\n\n\n","title":"JustPIC.centroid2particle!"},{"category":"function","location":"interpolations/#JustPIC.particle2centroid!","page":"Interpolations","text":"particle2centroid!(F, Fp, particles::Particles)\nparticle2centroid!(F, Fp, xci::NTuple, particles::Particles, di)\n\nInterpolate particle-centered values Fp to cell centers F.\n\nxci contains the 1D coordinate arrays of the cell centers. This is the cell-centered counterpart to particle2grid! and mutates F in place.\n\nArguments\n\nF: destination centroid array, or tuple of centroid arrays.\nFp: particle field stored with the same cell layout as particles.\nparticles: the Particles container supplying particle coordinates. Its stored xci coordinates define the target centroid grid.\nghost_1, ghost_2, ghost_3: whether F includes ghost nodes in each coordinate direction. Disable a keyword for a physical-only direction.\n\n\n\n\n\n","title":"JustPIC.particle2centroid!"},{"category":"section","location":"field_advection2D/#Field-advection-in-2D","page":"Field advection in 2D","text":"First, load JustPIC:\n\nusing JustPIC\n\nWe need to specify what backend are we running our simulation on. For convenience we define the backend as a constant. In this case we use the CPU backend, but we could also use the CUDA (CUDA.CUDABackend) or AMDGPU (AMDGPU.ROCBackend) backends.\n\nconst backend = JustPIC.CPU\n\nDefine an analytical flow used to advect the particles:\n\nvx_stream(x, y) =  250 * sin(π*x) * cos(π*y)\nvy_stream(x, y) = -250 * cos(π*x) * sin(π*y)\n\nDefine the model domain:\n\nn  = 256        # number of nodes\nnx = ny = n-1   # number of cells in x and y\nLx = Ly = 1.0   # domain size\nxvi = xv, yv = LinRange(0, Lx, n), LinRange(0, Ly, n) # cell vertices\ndxi = dx, dy = xv[2] - xv[1], yv[2] - yv[1] # cell size\nxci = xc, yc = LinRange(0+dx/2, Lx-dx/2, n-1), LinRange(0+dy/2, Ly-dy/2, n-1) # cell centers\n\nJustPIC uses staggered velocity grids, so we define one coordinate tuple for each velocity component. Each staggered direction carries one ghost cell on either side, which expand_range adds to a 1D coordinate range:\n\nfunction expand_range(x::AbstractRange)\n    dx = x[2] - x[1]\n    n = length(x)\n    x1, x2 = extrema(x)\n    xI = round(x1-dx; sigdigits=5)\n    xF = round(x2+dx; sigdigits=5)\n    LinRange(xI, xF, n + 2)\nend\n\ngrid_vx = xv, expand_range(yc) # staggered grid for Vx\ngrid_vy = expand_range(xc), yv # staggered grid for Vy\n\nNext, initialize the particles:\n\nnxcell    = 24 # initial number of particles per cell\nmax_xcell = 48 # maximum number of particles per cell\nmin_xcell = 14 # minimum number of particles per cell\nparticles = init_particles(\n    backend, nxcell, max_xcell, min_xcell, grid_vx, grid_vy\n)\n\nDefine the velocity field on the staggered grids and the scalar field on vertices:\n\nVx = TA(backend)([vx_stream(x, y) for x in grid_vx[1], y in grid_vx[2]]);\nVy = TA(backend)([vy_stream(x, y) for x in grid_vy[1], y in grid_vy[2]]);\nxvi_particles = Array.(particles.xvi)\nT  = TA(backend)([y for x in xvi_particles[1], y in xvi_particles[2]]); # includes periodic ghost nodes\nV  = Vx, Vy;\n\nTA(backend) converts the data to the array type associated with the selected backend.\n\nWe also need to initialize the field T on the particles\n\nparticle_args = pT, = init_cell_arrays(particles, Val(1));\n\nUse grid2particle! to interpolate T to the particles:\n\ngrid2particle!(pT, T, particles);\n\nWe can now start the time loop:\n\ndt = min(dx / maximum(abs.(Array(Vx))),  dy / maximum(abs.(Array(Vy)))) / 2\nniter = 250\nfor it in 1:niter\n    advection!(particles, RungeKutta2(), V, dt)                     # advect particles\n    move_particles!(particles, particle_args)                       # move particles in the memory\n    inject_particles!(particles, (pT, ))                            # inject particles if needed\n    particle2grid!(T, pT, particles)                                # interpolate particles to the grid\nend","title":"Field advection in 2D"},{"category":"section","location":"field_advection2D/#Pure-shear-in-2D","page":"Field advection in 2D","text":"An example of two-dimensional pure shear flow is provided in scripts/pureshear_ALE.jl. The velocity field is set to:\n\nv_x = dotvarepsilon x\n\nv_y = -dotvarepsilon y\n\nwhere dotvarepsilon is the pure shear strain rate applied at the boundaries. A positive value of dotvarepsilon leads to horizontal extension, while negative values correspond to horizontal compression.\n\nThe ALE switch (Arbitrary Lagrangian-Eulerian) controls model-box deformation. If ALE=false, the model dimensions remain constant over time. If ALE=true, the model domain deforms with the background pure-shear rate.","title":"Pure shear in 2D"},{"category":"section","location":"particles/#Particles","page":"Particles","text":"","title":"Particles"},{"category":"section","location":"particles/#Memory-layout","page":"Particles","text":"Particles and MarkerChain store coordinates in CellArrays from CellArrays.jl. Entries are grouped by parent cell so that interpolation, advection, and reinjection routines work on spatially local chunks of memory.","title":"Memory layout"},{"category":"section","location":"particles/#Particle-objects","page":"Particles","text":"JustPIC exposes three concrete AbstractParticles containers:\n\nParticles: the main cell-sorted container used for PIC workflows.\nPassiveMarkers: lightweight tracers stored as flat coordinate arrays.\nMarkerChain: a 2D interface-tracking container for free surfaces and similar boundaries.\n\nParticles stores coordinates, active-slot masks, occupancy thresholds, and the derived center, vertex, and velocity grids used by the high-level APIs. Those extra fields are what let helpers such as move_particles!, grid2particle!, particle2grid!, inject_particles!, update_phase_ratios!, and subgrid_diffusion! use the compact (..., particles, ...) call style.","title":"Particle objects"},{"category":"section","location":"particles/#Initialization","page":"Particles","text":"init_particles accepts either:\n\na scalar nxcell for random, quadrant-balanced seeding inside each cell, or\na tuple nxcell for a regular per-dimension layout.\n\nAfter construction, the returned Particles object already contains the center, vertex, and staggered velocity grids needed by the higher-level APIs.","title":"Initialization"},{"category":"section","location":"particles/#Randomly-distributed-particles","page":"Particles","text":"backend   = JustPIC.CPU # device backend\nnxcell    = 24  # initial number of randomly distributed particles\nmax_xcell = 48  # maximum number of particles per cell\nmin_xcell = 12  # minimum number of particles per cell\nn         = 32  # number of vertices per dimension\nLx   = Ly = 1.0 # domain size\nxvi       = xv, yv = LinRange(0, Lx, n), LinRange(0, Ly, n) # nodal vertices\ndxi       = dx, dy = xv[2] - xv[1], yv[2] - yv[1]\nxci       = xc, yc = LinRange(dx / 2, Lx - dx / 2, n - 1), LinRange(dy / 2, Ly - dy / 2, n - 1)\ngrid_vx   = xv, LinRange(first(yc) - dy, last(yc) + dy, length(yc) + 2)\ngrid_vy   = LinRange(first(xc) - dx, last(xc) + dx, length(xc) + 2), yv\n## initialize particles object with randomly distributed coordinates\nparticles = init_particles(\n    backend, nxcell, max_xcell, min_xcell, grid_vx, grid_vy,\n)","title":"Randomly distributed particles"},{"category":"section","location":"particles/#Regularly-spaced-particles","page":"Particles","text":"backend   = JustPIC.CPU # device backend\nnxcell    = (5, 5)  # number of evenly spaced particles in each cell dimension\nmax_xcell = 48      # maximum number of particles per cell\nmin_xcell = 12      # minimum number of particles per cell\nn         = 32      # number of cells per dimension\nLx   = Ly = 1.0     # domain size\nxvi       = xv, yv = LinRange(0, Lx, n), LinRange(0, Ly, n) # nodal vertices\ndxi       = dx, dy = xv[2] - xv[1], yv[2] - yv[1]\nxci       = xc, yc = LinRange(dx / 2, Lx - dx / 2, n - 1), LinRange(dy / 2, Ly - dy / 2, n - 1)\ngrid_vx   = xv, LinRange(first(yc) - dy, last(yc) + dy, length(yc) + 2)\ngrid_vy   = LinRange(first(xc) - dx, last(xc) + dx, length(xc) + 2), yv\n## initialize particles object with regularly distributed coordinates\nparticles = init_particles(\n    backend, nxcell, max_xcell, min_xcell, grid_vx, grid_vy,\n)","title":"Regularly spaced particles"},{"category":"section","location":"particles/#Particle-maintenance","page":"Particles","text":"These helpers keep the cell-sorted storage healthy as particles move: move_particles!, inject_particles!, inject_particles_phase!, clean_particles!, force_injection!, cell_index, and cell_length.\n\nOnce particles has been initialized, most transfer and maintenance routines use the geometry stored in the container directly:\n\ngrid2particle!(Fp, F, particles)\nparticle2grid!(F, Fp, particles)\nmove_particles!(particles, particle_args)\ninject_particles!(particles, particle_args)\nupdate_phase_ratios!(phase_ratios, particles, phases)\n\nThis is the preferred high-level style for simulation code, tests, and examples.","title":"Particle maintenance"},{"category":"section","location":"particles/#API","page":"Particles","text":"","title":"API"},{"category":"section","location":"particles/#Containers","page":"Particles","text":"","title":"Containers"},{"category":"section","location":"particles/#Initialization-2","page":"Particles","text":"","title":"Initialization"},{"category":"section","location":"particles/#Advection","page":"Particles","text":"","title":"Advection"},{"category":"section","location":"particles/#Maintenance","page":"Particles","text":"","title":"Maintenance"},{"category":"section","location":"particles/#Phase-ratios-and-subgrid-diffusion","page":"Particles","text":"","title":"Phase ratios and subgrid diffusion"},{"category":"type","location":"particles/#JustPIC.Particles","page":"Particles","text":"Particles{Backend, N, I, T1, T2, D, V} <: AbstractParticles\n\nMain particle container used by JustPIC for material points stored cell-by-cell in CellArrays.\n\ncoords is an N-tuple of particle-coordinate arrays, index marks which slots are active inside each cell, nxcell is the target initial occupancy per cell, and min_xcell/max_xcell define the occupancy range used by injection and cleanup routines.\n\nUse init_particles to construct this type instead of calling the inner constructor directly.\n\n\n\n\n\n","title":"JustPIC.Particles"},{"category":"type","location":"particles/#JustPIC.PassiveMarkers","page":"Particles","text":"PassiveMarkers{Backend,T} <: AbstractParticles\n\nLightweight particle container for passive tracers that only store coordinates.\n\nUnlike Particles, passive markers do not keep per-cell occupancy metadata and are intended for tracer-style advection and interpolation workflows where the markers do not feed back into the simulation.\n\nUse init_passive_markers to construct this type.\n\n\n\n\n\n","title":"JustPIC.PassiveMarkers"},{"category":"function","location":"particles/#JustPIC.init_particles","page":"Particles","text":"init_particles(backend, nxcell, max_xcell, min_xcell, grid_vx, grid_vy[, grid_vz])\n\nInitialize a Particles container from the staggered velocity grids.\n\nEach velocity component is supplied as an N-tuple of coordinate vectors. The diagonal coordinate vector of each component defines the particle vertex grid; the off-diagonal vectors define the cell-center grid. For example, in 2D pass grid_vx = (xv, yc_extended) and grid_vy = (xc_extended, yv).\n\nIf nxcell is a number, particles are distributed randomly within cell quadrants. If it is an NTuple, it is interpreted as the number of particles to place regularly along each coordinate direction within every cell.\n\nThe particle vertex and center grids stored in the returned container are extended with periodic ghost nodes. The staggered velocity grids are stored as provided.\n\nArguments\n\nbackend: KernelAbstractions backend type such as CPU.\nnxcell: either the target number of particles per cell, or an NTuple describing a structured per-dimension layout.\nmax_xcell: number of particle slots reserved per cell.\nmin_xcell: minimum occupancy used by reinjection routines.\ngrid_vx, grid_vy, grid_vz: staggered velocity-grid coordinate tuples. Omit grid_vz for a 2D simulation. Each tuple must contain one coordinate vector per spatial dimension.\n\nReturns\n\nA Particles object whose coordinates and occupancy arrays are ready for advection/interpolation routines, with particles.xvi and particles.xci including one periodic ghost node on each side.\n\nExample\n\nxv, yv = LinRange(0, 1, 33), LinRange(0, 1, 33)\ndx = xv[2] - xv[1]\nxc = LinRange(dx / 2, 1 - dx / 2, 32)\nyc = xc\ngrid_vx = xv, LinRange(first(yc) - dx, last(yc) + dx, 34)\ngrid_vy = LinRange(first(xc) - dx, last(xc) + dx, 34), yv\nparticles = init_particles(CPU, 24, 48, 12, grid_vx, grid_vy)\n\n\n\n\n\n","title":"JustPIC.init_particles"},{"category":"function","location":"particles/#JustPIC.init_passive_markers","page":"Particles","text":"init_passive_markers(backend, coords::NTuple{N,AbstractArray})\n\nConstruct a PassiveMarkers container on backend from marker coordinate arrays.\n\ncoords is an N-tuple of vectors, one per spatial dimension, holding the initial marker positions: marker k sits at (coords[1][k], …, coords[N][k]).\n\nArguments\n\nbackend: KernelAbstractions backend type such as CPU.\ncoords: tuple of coordinate vectors, one per dimension.\n\n\n\n\n\n","title":"JustPIC.init_passive_markers"},{"category":"function","location":"particles/#JustPIC.init_cell_arrays","page":"Particles","text":"init_cell_arrays(particles::Particles, ::Val{N})\n\nAllocate N cell-aligned scratch arrays with the same cell layout as particles.coords.\n\nThis is mainly used internally to create per-particle temporary storage for quantities such as interpolated fields or time-integration work arrays.\n\nReturns\n\nAn N-tuple of CellArrays with the same particle-cell layout as particles.coords.\n\n\n\n\n\n","title":"JustPIC.init_cell_arrays"},{"category":"type","location":"particles/#JustPIC.AbstractAdvectionIntegrator","page":"Particles","text":"AbstractAdvectionIntegrator\n\nAbstract supertype for time integrators used by particle, passive-marker, and marker-chain advection routines.\n\n\n\n\n\n","title":"JustPIC.AbstractAdvectionIntegrator"},{"category":"type","location":"particles/#JustPIC.Euler","page":"Particles","text":"Euler()\n\nForward-Euler advection integrator.\n\nThis is the cheapest available integrator and is mainly useful for simple tests or when first-order accuracy is sufficient.\n\n\n\n\n\n","title":"JustPIC.Euler"},{"category":"type","location":"particles/#JustPIC.RungeKutta2","page":"Particles","text":"RungeKutta2(α = 0.5)\n\nSecond-order Runge-Kutta advection integrator.\n\nThe parameter α controls the intermediate stage location and must satisfy 0 < α < 1. The default α = 0.5 corresponds to the midpoint method.\n\n\n\n\n\n","title":"JustPIC.RungeKutta2"},{"category":"type","location":"particles/#JustPIC.RungeKutta4","page":"Particles","text":"RungeKutta4()\n\nClassical fourth-order Runge-Kutta advection integrator.\n\n\n\n\n\n","title":"JustPIC.RungeKutta4"},{"category":"function","location":"particles/#JustPIC.set_precision","page":"Particles","text":"set_precision(integrator, T)\n\nRecast an integrator's stored parameters to the scalar precision T.\n\nThis is applied at the advection launch sites so that a Float32 backend (such as Metal, which has no Float64) never carries a Float64 field into a GPU kernel. It is the identity for parameter-free integrators and, on the Float64 CPU/CUDA/AMDGPU path, a no-op (the value is preserved).\n\n\n\n\n\n","title":"JustPIC.set_precision"},{"category":"function","location":"particles/#JustPIC.advection!","page":"Particles","text":"advection!(particles::Particles, method::AbstractAdvectionIntegrator, V, dt)\nadvection!(particles::Particles, method::AbstractAdvectionIntegrator, V, grid_vi, dt, dxi)\n\nAdvect particles through the staggered velocity field V over a time step dt. The particle coordinates are updated in place.\n\nThe public form reads the staggered velocity coordinate grids and spacing from particles (particles.xi_vel and particles.di.velocity), so only V and dt are supplied. The lower-level form takes those grids explicitly.\n\nArguments\n\nparticles: Particles container to advect.\nmethod: time integrator such as Euler(), RungeKutta2(), or RungeKutta4().\nV: tuple of staggered velocity component arrays.\ndt: timestep.\ngrid_vi: tuple of coordinate tuples matching the staggering of V (lower-level form only).\ndxi: grid spacing associated with grid_vi (lower-level form only).\nperiodic_1, periodic_2, periodic_3: enable periodic wrapping at every integration stage in the corresponding coordinate direction.\n\nNotes\n\nUse the same periodic keywords in the subsequent move_particles! call.\nStage-wise wrapping is required by RungeKutta2 and RungeKutta4, whose intermediate interpolation points may cross a periodic boundary.\n\n\n\n\n\nadvection!(chain::MarkerChain, method, V, grid_vi, dt)\n\nAdvect the marker coordinates in chain through the staggered velocity field V without performing resampling or topography reconstruction.\n\nThis lower-level method is useful if you want to customize the post-advection marker-chain processing yourself.\n\n\n\n\n\nadvection!(particles::PassiveMarkers, method::AbstractAdvectionIntegrator, V, grid_vxi, dt)\n\nAdvect passive marker coordinates through the staggered velocity field V over a time step dt. The marker coordinates are updated in place.\n\nUnlike the Particles method, grid_vxi must be supplied explicitly, since PassiveMarkers stores only marker coordinates and no grid metadata.\n\nArguments\n\nparticles: PassiveMarkers container to advect.\nmethod: time integrator such as Euler(), RungeKutta2(), or RungeKutta4().\nV: tuple of staggered velocity component arrays.\ngrid_vxi: tuple of coordinate tuples matching the staggering of V.\ndt: timestep.\n\n\n\n\n\n","title":"JustPIC.advection!"},{"category":"function","location":"particles/#JustPIC.advection_LinP!","page":"Particles","text":"advection_LinP!(particles, method, V, dt; periodic_1=false, periodic_2=false, periodic_3=false)\n\nAdvect particles using the linear-plus-pressure (LinP) velocity interpolation scheme.\n\nThis variant uses the same time integrators as advection! but evaluates velocities with the LinP reconstruction near staggered pressure points.\n\nThis method is useful when you want the interpolation behavior described in the velocity-interpolation documentation under LinP.\n\nPeriodic keywords have the same meaning as in advection! and must also be passed to the subsequent move_particles! call.\n\n\n\n\n\n","title":"JustPIC.advection_LinP!"},{"category":"function","location":"particles/#JustPIC.advection_MQS!","page":"Particles","text":"advection_MQS!(particles, method, V, dt; periodic_1=false, periodic_2=false, periodic_3=false)\n\nAdvect particles using the monotonic quadratic spline (MQS) velocity interpolation scheme.\n\nCompared with advection!, this method reconstructs staggered velocities with MQS where enough stencil support is available.\n\nNear boundaries or when the required stencil is unavailable, the implementation falls back to linear interpolation.\n\nThe public entry point reads the staggered velocity coordinates and spacing from particles.xi_vel and particles.di.velocity.\n\nPeriodic keywords have the same meaning as in advection! and must also be passed to the subsequent move_particles! call.\n\n\n\n\n\n","title":"JustPIC.advection_MQS!"},{"category":"function","location":"particles/#JustPIC.semilagrangian_advection!","page":"Particles","text":"semilagrangian_advection!(F, F0, method, V, grid_vi, grid, dt)\n\nAdvect a grid field with a semi-Lagrangian backtracking step.\n\nEach destination node in F is traced backward through the velocity field V, then sampled from F0 on the vertex grid grid. grid_vi contains the staggered coordinates associated with the velocity components.\n\nNotes\n\nF is overwritten in place.\nF0 is the source field from the previous step.\nFor tuple-valued fields, each component is backtracked independently.\n\n\n\n\n\nsemilagrangian_advection!(chain::MarkerChain, method, V, grid_vxi, grid, dt)\n\nAdvance only the vertex topography chain.h_vertices by one semi-Lagrangian step.\n\nEach new vertex height is found by backtracking through the velocity field V (so method must support backtracking, i.e. RungeKutta2/RungeKutta4, not Euler). This is the raw update used by semilagrangian_advection_markerchain!; it does not apply slope limiting, mass conservation, or marker reconstruction — call the wrapper unless you need to compose those steps yourself. Departures outside the horizontal chain domain sample the nearest endpoint height; velocity interpolation extrapolates from edge cells. The old surface is piecewise linear, so RK order describes trajectory integration, not the spatial interpolation order. A failed characteristic solve throws an error without changing the chain.\n\n\n\n\n\n","title":"JustPIC.semilagrangian_advection!"},{"category":"function","location":"particles/#JustPIC.semilagrangian_advection_LinP!","page":"Particles","text":"semilagrangian_advection_LinP!(F, F0, method, V, grid_vi, grid, dt)\n\nSemi-Lagrangian advection variant that evaluates backtracked velocities with the LinP interpolation scheme.\n\nUse this when the advecting velocity should be reconstructed with the LinP scheme instead of plain linear interpolation.\n\n\n\n\n\n","title":"JustPIC.semilagrangian_advection_LinP!"},{"category":"function","location":"particles/#JustPIC.semilagrangian_advection_MQS!","page":"Particles","text":"semilagrangian_advection_MQS!(F, F0, method, V, grid_vi, grid, dt)\n\nSemi-Lagrangian advection variant that evaluates backtracked velocities with the MQS interpolation scheme.\n\nUse this when the advecting velocity should be reconstructed with the MQS scheme instead of plain linear interpolation.\n\n\n\n\n\n","title":"JustPIC.semilagrangian_advection_MQS!"},{"category":"function","location":"particles/#JustPIC.move_particles!","page":"Particles","text":"move_particles!(particles::AbstractParticles, args; periodic_1=false, periodic_2=false, periodic_3=false)\nmove_particles!(particles::AbstractParticles, grid, args, dxi; periodic_1=false, periodic_2=false, periodic_3=false)\n\nReassign particles to the correct parent cells after their coordinates have been updated.\n\nThis routine keeps the coordinate arrays in particles and the companion fields in args sorted by parent cell, preserving the package's spatially local memory layout.\n\nArguments\n\nparticles: particle container whose coordinates have already been modified.\nargs: tuple of per-particle fields that must move together with the particle coordinates.\ngrid: optional vertex grid coordinates used by the lower-level method.\ndxi: optional grid spacing used by the lower-level method.\nperiodic_1, periodic_2, periodic_3: enable periodic wrapping in the corresponding coordinate direction.\n\nNotes\n\nParticles that leave a non-periodic direction are discarded.\nPeriodic directions use the ghost cells created by add_periodic_ghost_nodes to wrap coordinates and particle fields across opposite domain boundaries.\nargs must use the same cell layout as particles.coords.\nThe public entry point uses the vertex grid and spacing stored in particles.\n\n\n\n\n\nmove_particles!(chain::MarkerChain)\n\nReassign markers to the correct columns of chain after their coordinates have been updated.\n\nMarkers that crossed column boundaries are moved into their destination column's slots, keeping the coordinate arrays consistent with the per-column occupancy mask. A marker may cross any number of columns in one call. Markers whose updated coordinates are not finite, or which left the horizontal extent of chain.cell_vertices, are deleted.\n\n\n\n\n\n","title":"JustPIC.move_particles!"},{"category":"function","location":"particles/#JustPIC.inject_particles!","page":"Particles","text":"inject_particles!(particles::Particles, args)\n\nInject particles into cells whose occupancy falls below particles.min_xcell.\n\nArguments\n\nparticles: The particles object.\nargs: tuple of particle fields that should be populated for newly injected particles.\n\nNotes\n\nNew particles are placed quadrant-by-quadrant inside the cell.\nNew field values are copied from the nearest existing particle in the same neighborhood.\nThe public entry point uses the vertex grid and cell spacing stored in particles.\n\n\n\n\n\n","title":"JustPIC.inject_particles!"},{"category":"function","location":"particles/#JustPIC.inject_particles_phase!","page":"Particles","text":"inject_particles_phase!(particles, particles_phases, args, fields, grid)\n\nInject particles into under-populated cells while also copying phase labels and field values from nearby particles.\n\nThis is the phase-aware variant of inject_particles!.\n\nparticles_phases stores a phase id per particle slot, while args/fields hold companion particle properties that must be initialized consistently for the new particles.\n\n\n\n\n\n","title":"JustPIC.inject_particles_phase!"},{"category":"function","location":"particles/#JustPIC.clean_particles!","page":"Particles","text":"clean_particles!(particles, grid, args)\n\nRemove invalid or inactive particle slots and keep particle-associated fields in args consistent with the particle storage layout.\n\nThis is typically used after particle deletion or reinjection to compact each cell's active particle block.\n\n\n\n\n\n","title":"JustPIC.clean_particles!"},{"category":"function","location":"particles/#JustPIC.force_injection!","page":"Particles","text":"force_injection!(particles, p_new, fields, values)\n\nInsert particles from p_new directly into free particle slots.\n\nArguments\n\nparticles: destination Particles container.\np_new: per-cell collection of coordinates to inject; NaN marks empty input slots.\nfields: tuple of particle fields to initialize together with the coordinates.\nvalues: values written into each corresponding entry of fields.\n\nNotes\n\nThis is a low-level routine: it does not search for nearest-neighbor values.\nInjection only happens into currently inactive particle slots.\n\n\n\n\n\nforce_injection!(particles, p_new)\n\nConvenience method for force_injection! when no companion particle fields need to be initialized.\n\n\n\n\n\n","title":"JustPIC.force_injection!"},{"category":"type","location":"particles/#JustPIC.PhaseRatios","page":"Particles","text":"PhaseRatios{Backend,T}\n\nStorage for phase-fraction fields sampled at multiple grid locations.\n\nDepending on dimension, the container holds phase ratios at cell centers, vertices, staggered velocity nodes, and in 3D also at edge midpoints.\n\nThe fields store, for each location, the fractional occupancy of each material phase inferred from particle labels.\n\n\n\n\n\n","title":"JustPIC.PhaseRatios"},{"category":"function","location":"particles/#JustPIC.nphases","page":"Particles","text":"nphases(x::PhaseRatios)\n\nReturn the number of phases in x::PhaseRatios.\n\nThis method returns a Val wrapper for the phase count; use numphases when you need the integer directly.\n\n\n\n\n\n","title":"JustPIC.nphases"},{"category":"function","location":"particles/#JustPIC.subgrid_diffusion!","page":"Particles","text":"subgrid_diffusion!(pT, T_grid, ΔT_grid, subgrid_arrays, particles, dt; d = 1.0)\n\nApply the vertex-based subgrid diffusion correction to particle temperatures.\n\nTemperatures are interpolated from the grid to particles, relaxed using the local subgrid model, mapped back to the grid as a correction, and then reapplied to the particle temperatures.\n\nArguments\n\npT: particle temperature field updated in place.\nT_grid: source temperature on the ghosted vertex grid, sized as length.(particles.xvi).\nΔT_grid: resolved-grid temperature increment carrying one ghost node per side, sized ncells .+ 2.\nsubgrid_arrays: scratch storage created with SubgridDiffusionCellArrays(particles).\nparticles: particle container.\ndt: timestep.\nd: dimensionless subgrid diffusion coefficient.\n\n\n\n\n\n","title":"JustPIC.subgrid_diffusion!"},{"category":"function","location":"particles/#JustPIC.subgrid_diffusion_centroid!","page":"Particles","text":"subgrid_diffusion_centroid!(pT, T_grid, ΔT_grid, subgrid_arrays, particles, dt; d = 1.0)\n\nCentroid-grid variant of subgrid_diffusion!.\n\nUse this when the resolved temperature field lives at cell centers instead of vertices. T_grid is then the ghosted centroid field sized as length.(particles.xci), while ΔT_grid keeps the same ncells .+ 2 layout as in subgrid_diffusion!.\n\n\n\n\n\n","title":"JustPIC.subgrid_diffusion_centroid!"},{"category":"type","location":"particles/#JustPIC.SubgridDiffusionCellArrays","page":"Particles","text":"SubgridDiffusionCellArrays(particles; loc = :vertex)\n\nAllocate scratch storage used by the subgrid thermal diffusion routines.\n\nThe returned object stores old particle temperatures, per-particle temperature increments, characteristic diffusion timescales, and a grid-sized accumulation buffer.\n\nloc selects whether the accumulation buffer should match a vertex-based (:vertex) or cell-centered (:center) grid layout. Either way the buffer is ghosted like particles.xvi/particles.xci.\n\n\n\n\n\n","title":"JustPIC.SubgridDiffusionCellArrays"},{"category":"section","location":"field_advection3D/#Field-advection-in-3D","page":"Field advection in 3D","text":"First, load JustPIC:\n\nusing JustPIC\n\nWe need to specify what backend are we running our simulation on. For convenience we define the backend as a constant. In this case we use the CPU backend, but we could also use the CUDA (CUDA.CUDABackend) or AMDGPU (AMDGPU.ROCBackend) backends.\n\nconst backend = JustPIC.CPU\n\nDefine an analytical flow used to advect the particles:\n\nvx_stream(x, z) =  250 * sin(π*x) * cos(π*z)\nvy_stream(x, z) =  0.0\nvz_stream(x, z) = -250 * cos(π*x) * sin(π*z)\n\nDefine the model domain:\n\nn  = 64             # number of nodes\nnx  = ny = nz = n-1 # number of cells in x and y\nLx  = Ly = Lz = 1.0 # domain size\nni  = nx, ny, nz\nLi  = Lx, Ly, Lz\n\nxvi = xv, yv, zv = ntuple(i -> LinRange(0, Li[i], n), Val(3)) # cell vertices\ndxi = dx, dy, dz = ntuple(i -> xvi[i][2] - xvi[i][1], Val(3)) # cell size\nxci = xc, yc, zc = ntuple(i -> LinRange(0+dxi[i]/2, Li[i]-dxi[i]/2, ni[i]), Val(3)) # cell centers\n\nJustPIC uses staggered velocity grids, so we define one coordinate tuple for each velocity component. Each staggered direction carries one ghost cell on either side, which expand_range adds to a 1D coordinate range:\n\nfunction expand_range(x::AbstractRange)\n    dx = x[2] - x[1]\n    n = length(x)\n    x1, x2 = extrema(x)\n    xI = round(x1-dx; sigdigits=5)\n    xF = round(x2+dx; sigdigits=5)\n    LinRange(xI, xF, n+2)\nend\n\ngrid_vx = xv              , expand_range(yc), expand_range(zc) # staggered grid for Vx\ngrid_vy = expand_range(xc), yv              , expand_range(zc) # staggered grid for Vy\ngrid_vz = expand_range(xc), expand_range(yc), zv               # staggered grid for Vz\n\nNext, initialize the particles:\n\nnxcell    = 24 # initial number of particles per cell\nmax_xcell = 48 # maximum number of particles per cell\nmin_xcell = 14 # minimum number of particles per cell\nparticles = init_particles(\n    backend, nxcell, max_xcell, min_xcell, grid_vx, grid_vy, grid_vz\n)\n\nDefine the velocity field on the staggered grids and the scalar field on vertices:\n\nVx = TA(backend)([vx_stream(x, z) for x in grid_vx[1], y in grid_vx[2], z in grid_vx[3]])\nVy = TA(backend)([vy_stream(x, z) for x in grid_vy[1], y in grid_vy[2], z in grid_vy[3]])\nVz = TA(backend)([vz_stream(x, z) for x in grid_vz[1], y in grid_vz[2], z in grid_vz[3]])\nxvi_particles = Array.(particles.xvi)\nT  = TA(backend)([z for x in xvi_particles[1], y in xvi_particles[2], z in xvi_particles[3]]) # includes periodic ghost nodes\nV  = Vx, Vy, Vz\n\nTA(backend) converts the data to the array type associated with the selected backend.\n\nWe also need to initialize the field T on the particles\n\nparticle_args = pT, = init_cell_arrays(particles, Val(1));\n\nUse grid2particle! to interpolate T to the particles:\n\ngrid2particle!(pT, T, particles)\n\nWe can now start the time loop:\n\ndt = min(dx / maximum(abs.(Vx)), dy / maximum(abs.(Vy)), dz / maximum(abs.(Vz))) / 2\n\nniter = 250\nfor it in 1:niter\n    advection!(particles, RungeKutta2(), V, dt)                               # advect particles\n    move_particles!(particles, particle_args)                                # move particles in the memory\n    inject_particles!(particles, (pT, ))                                     # inject particles if needed\n    particle2grid!(T, pT, particles)                                         # interpolate particles to the grid\nend","title":"Field advection in 3D"},{"category":"section","location":"#JustPIC.jl","page":"Home","text":"Particle-in-Cell (PIC) advection for large-scale, multi-XPU simulations. JustPIC runs the same code on CPU, CUDA, AMDGPU, and Metal through KernelAbstractions: the compute backend is chosen by the array type, not by rewriting the algorithm.","title":"JustPIC.jl"},{"category":"section","location":"#Installation","page":"Home","text":"JustPIC is registered in the General registry:\n\nusing Pkg\nPkg.add(\"JustPIC\")\n\nTo run on a GPU, additionally install the matching backend package (CUDA, AMDGPU, or Metal) and select its backend — see Mixed CPU/GPU.","title":"Installation"},{"category":"section","location":"#Getting-started","page":"Home","text":"Choose a backend, build a staggered grid, and initialize a particle container:\n\njulia> using JustPIC\n\njulia> backend = JustPIC.CPU;\n\njulia> nxcell, max_xcell, min_xcell = 6, 12, 3;\n\njulia> n = 8;  # vertices per dimension\n\njulia> xv = yv = LinRange(0.0, 1.0, n);\n\njulia> dx = dy = xv[2] - xv[1];\n\njulia> xc = yc = LinRange(dx / 2, 1 - dx / 2, n - 1);\n\njulia> grid_vx = xv, LinRange(first(yc) - dy, last(yc) + dy, length(yc) + 2);\n\njulia> grid_vy = LinRange(first(xc) - dx, last(xc) + dx, length(xc) + 2), yv;\n\njulia> particles = init_particles(backend, nxcell, max_xcell, min_xcell, grid_vx, grid_vy);\n\njulia> sum(particles.index.data)  # number of active markers across the grid\n392\n\nparticles can then be advected through a velocity field and used for grid-to-particle and particle-to-grid interpolation. See Particles and Interpolations for the full workflow, or the runnable 2D field advection example.","title":"Getting started"},{"category":"section","location":"#Manual","page":"Home","text":"Particles — the Particles container and its lifecycle\nCellArrays — the cell-local storage layout\nInterpolations and Velocity interpolation\nMarker chain — free-surface / topography tracking\nI/O — checkpointing and restart\nMixed CPU/GPU\nPublic API","title":"Manual"},{"category":"section","location":"#Funding","page":"Home","text":"The development of this package is supported by the GPU4GEO PASC project.","title":"Funding"},{"category":"section","location":"field_advection2D_MPI/#Field-advection-in-2D-using-MPI","page":"Field advection in 2D using MPI","text":"This example uses periodic boundaries in both coordinate directions. The local particle grids therefore include periodic ghost nodes, and MPI halo exchanges must happen at the right points of the advection loop so that particles and their fields remain consistent across rank boundaries.\n\nAs usual, we start loading JustPIC.jl modules and specifying the backend\n\nusing JustPIC\nconst backend = JustPIC.CPU\n\nand we define the usual analytical flow solution\n\nvx_stream(x, y) =  250 * sin(π*x) * cos(π*y)\nvy_stream(x, y) = -250 * cos(π*x) * sin(π*y)\n\nThis time, we also need to load MPI.jl and ImplicitGlobalGrid.jl to handle MPI communication between CPU ranks.\n\nusing ImplicitGlobalGrid\nusing MPI: MPI\n\nThen we define the model domain:\n\nn  = 256        # number of nodes\nnx = ny = n-1   # number of cells in x and y\nLx = Ly = 1.0   # domain size\n\nInitialize the global grid:\n\nme, dims, = init_global_grid(\n    n-1, n-1, 1;\n    periodx=1,\n    periody=1,\n    init_MPI=MPI.Initialized() ? false : true,\n)\ndxi = dx, dy = Lx /(nx_g()-1), Ly / (ny_g()-1)\n\nand the arrays local to each MPI rank\n\n# nodal vertices\nxvi = xv, yv = let\n    dummy = zeros(n, n) \n    xv  = TA(backend)([x_g(i, dx, dummy) for i in axes(dummy, 1)])\n    yv  = TA(backend)([y_g(i, dx, dummy) for i in axes(dummy, 2)])\n    xv, yv\nend\n# nodal centers\nxci = xc, yc = let\n    dummy = zeros(nx, ny) \n    xc  = TA(backend)([x_g(i, dx, dummy) for i in axes(dummy, 1)])\n    yc  = TA(backend)([y_g(i, dx, dummy) for i in axes(dummy, 2)])\n    xc, yc\nend\n# staggered grid for the velocity components\ngrid_vx = xv, add_ghost_nodes(yc, dy, (0.0, Ly))\ngrid_vy = add_ghost_nodes(xc, dx, (0.0, Lx)), yv\n\nAnd we continue with business as usual\n\nnxcell    = 24 # initial number of particles per cell\nmax_xcell = 48 # maximum number of particles per cell\nmin_xcell = 14 # minimum number of particles per cell\nparticles = init_particles(\n    backend, nxcell, max_xcell, min_xcell, grid_vx, grid_vy\n)\n\nand the velocity and field we want to advect (on the staggered grid)\n\nVx = TA(backend)([vx_stream(x, y) for x in grid_vx[1], y in grid_vx[2]]);\nVy = TA(backend)([vy_stream(x, y) for x in grid_vy[1], y in grid_vy[2]]);\nxvi_particles = Array.(particles.xvi)\nT = TA(backend)([\n    y for x in xvi_particles[1], y in xvi_particles[2]\n])\nV  = Vx, Vy;\ndt = min(\n    dx / MPI.Allreduce(maximum(abs.(Vx)), MPI.MAX, MPI.COMM_WORLD),\n    dy / MPI.Allreduce(maximum(abs.(Vy)), MPI.MAX, MPI.COMM_WORLD)\n)\n\nNote that now we need to reduce over all the MPI ranks to compute the time-step.\n\nWe finally initialize the field T on the particles\n\nparticle_args = pT, = init_cell_arrays(particles, Val(1));\n\nand we use the function grid2particle! to interpolate the field T to the particles\n\ngrid2particle!(pT, T, particles);\n\nNow start the simulation\n\nniter = 250\nfor it in 1:niter\n    # advect particles\n    advection!(particles, RungeKutta2(), V, dt)\n    # exchange MPI halos before shuffling particles between parent cells\n    update_cell_halo!(particles.coords..., particle_args..., particles.index)\n    # move particles in memory\n    move_particles!(particles, particle_args)\n    # inject particles if needed\n    inject_particles!(particles, (pT, ))\n    # refresh halos again so reinjected particles are visible across ranks\n    update_cell_halo!(particles.coords..., particle_args..., particles.index)\n    # interpolate particles to the grid\n    particle2grid!(T, pT, particles)\nend","title":"Field advection in 2D using MPI"},{"category":"section","location":"field_advection2D_MPI/#Periodic-Boundary-Conditions","page":"Field advection in 2D using MPI","text":"For periodic MPI runs, there are two separate mechanisms working together:\n\ninit_particles extends the particle vertex and center grids with periodic ghost nodes.\nupdate_cell_halo! exchanges the overlapping CellArray halos between MPI ranks, including across periodic rank boundaries configured by init_global_grid. You can pass all coordinate, index, and per-particle field CellArrays in one call.\n\nThe usual pattern is:\n\nadvect particles locally\nexchange halos with update_cell_halo!\ncall move_particles! so particles that crossed a rank boundary are reassigned on the neighboring rank\nreinject particles if needed\nexchange halos again before particle2grid!\n\nIn code:\n\nadvection!(particles, RungeKutta2(), V, dt)\nupdate_cell_halo!(particles.coords..., particle_args..., particles.index)\nmove_particles!(particles, particle_args)\ninject_particles!(particles, (pT,))\nupdate_cell_halo!(particles.coords..., particle_args..., particles.index)\nparticle2grid!(T, pT, particles)\n\nIf the second halo refresh is skipped after reinjection, particle2grid! can reconstruct grid nodes from incomplete cross-rank neighborhoods. If the first halo refresh is skipped before move_particles!, particles that leave a rank can fail to appear on the neighboring rank.","title":"Periodic Boundary Conditions"},{"category":"section","location":"field_advection2D_MPI/#Visualization","page":"Field advection in 2D using MPI","text":"To visualize the results, we need to allocate a global array T_v and buffer arrays T_nohalo without the overlapping halo (here with width = 1)\n\nnx_v     = (size(T, 1) - 2) * dims[1] # global size of `T` without halos\nny_v     = (size(T, 2) - 2) * dims[2] # global size of `T` without halos\nT_v      = zeros(nx_v, ny_v)          # initialize global `T`\nT_nohalo = TA(backend)(zeros(size(T) .- 2)) # local `T` without overlapping halo\n\nVisualization with GLMakie.jl\n\nusing GLMakie\nx_global = range(0, Lx, length=size(T_v,1))\ny_global = range(0, Ly, length=size(T_v,2))\nheatmap(x_global, y_global, T_v)","title":"Visualization"},{"category":"section","location":"field_advection2D_MPI/#Going-3D","page":"Field advection in 2D using MPI","text":"A 3D example using MPI is found in scripts/temperature_advection3D_MPI.jl.","title":"Going 3D"},{"category":"section","location":"mixed_CPU_GPU/#Mixed-CPU-and-GPU-computations","page":"Mixed GPU/CPU","text":"If GPU memory is a limiting factor for your computation, it may be preferable to carry out particle operations on the CPU rather than on the GPU. This involves basically four steps:\n\nAt the top of the script. The JustPIC backend must be set to CPU, while other packages may still run their own GPU work:\n\nconst backend = JustPIC.CPU \n\nAt memory allocation stage. A copy of relevant CPU arrays must be allocated on the GPU memory. For example, phase ratios on mesh vertices:\n\nusing JustPIC\nusing CUDA\n\nphv_GPU = cell_array(CUDA.CUDABackend, 0.0, (N_phases,), (nx + 1, ny + 1, nz + 1))\n\nwhere N_phases is the number of different material phases and cell_array(CUDA.CUDABackend, ...) allocates a GPU-backed CellArray.\n\nSimilarly, GPU arrays must be copied to CPU memory:\n\nV_CPU = (\n    x = zeros(nx+1, ny+2, nz+2),\n    y = zeros(nx+2, ny+1, nz+2),\n    z = zeros(nx+2, ny+2, nz+1),\n)","title":"Mixed CPU and GPU computations"},{"category":"section","location":"mixed_CPU_GPU/#3.-Copy-particle-derived-fields-to-the-GPU","page":"Mixed GPU/CPU","text":"At each time step. The particles are stored in CPU memory. It is hence necessary to transfer some information from the CPU to the GPU memory. For example, here's a transfer of phase proportions:\n\nphv_GPU.data .= CuArray(phase_ratios.vertex).data\n\nAt each time step. Once velocity computations are finalized on the GPU, they need to be transferred to the CPU:\n\nV_CPU.x .= TA(backend)(V.x)\nV_CPU.y .= TA(backend)(V.y)\nV_CPU.z .= TA(backend)(V.z)\n\nAdvection can then be applied with the usual JustPIC API:\n\nadvection!(particles, RungeKutta2(), values(V), Δt)\n\nBecause backend == JustPIC.CPU, TA(backend) converts the velocity fields to plain CPU arrays before particle advection.","title":"3. Copy particle-derived fields to the GPU"},{"category":"section","location":"marker_chain/#Marker-chain","page":"Marker chain","text":"In geodynamic modeling it is often useful to track interfaces between different materials, such as a topographic profile or a lithological boundary. JustPIC.jl provides the MarkerChain object to define and advect open surfaces (not closed polygons) in two-dimensional models. A marker chain represents a single-valued height field y = h(x): markers are distributed along a horizontal grid and can only describe surfaces that do not fold back on themselves.","title":"Marker chain"},{"category":"section","location":"marker_chain/#The-MarkerChain-object","page":"Marker chain","text":"A MarkerChain stores its markers in the same column-wise CellArray layout used by Particles: the horizontal grid cell_vertices (xv) divides the domain into columns, and each column holds up to max_xcell marker slots guarded by a boolean occupancy mask. Alongside the markers, the chain caches the topography sampled at the grid vertices (h_vertices). The most useful fields are:\n\nField Meaning\ncoords marker coordinates, coords[1] = x, coords[2] = y (empty slots are NaN)\ncoords0 marker coordinates from the previous time step\nh_vertices topography at the grid vertices (current step)\nh_vertices0 vertex topography from the previous step (used for mass conservation)\ncell_vertices the horizontal grid xv defining the columns\nindex per-slot occupancy mask\nmin_xcell / max_xcell minimum / maximum markers allowed per column\n\nMarker precision follows the grid/elevation element type, so a Float32 grid produces a Float32 chain — this is required on Metal, which has no Float64.\n\ncell_vertices must be finite and strictly increasing; construction throws an ArgumentError otherwise. The spacing is free: a refined (non-uniformly spaced) grid is supported, and marker lookup, resampling and movement all resolve each column's width from its own pair of vertices. cell_length(chain, i) returns that width. The one-argument cell_length(chain) returns the single width shared by every column and therefore throws on a refined grid.","title":"The MarkerChain object"},{"category":"section","location":"marker_chain/#Creating-a-chain","page":"Marker chain","text":"Instantiate a chain with a constant elevation h:\n\nchain = init_markerchain(backend, nxcell, min_xcell, max_xcell, xv, h)\n\nwhere backend is the device backend, nxcell is the initial number of markers per column, min_xcell/max_xcell are the minimum/maximum markers allowed per column, and xv is the grid of vertices. h may also be a vector giving the initial elevation at each vertex.\n\nYou can overwrite the geometry of an existing chain in two ways. From an arbitrary topographic polyline:\n\n# create a topographic profile spanning the chain's horizontal extent\ntopo_x = LinRange(0, 1, 200)\ntopo_y = @. sin(2π * topo_x) * 0.1\n\nfill_chain_from_chain!(chain, topo_x, topo_y)\n\nor directly from heights defined at the grid vertices:\n\nfill_chain_from_vertices!(chain, topo_y)   # length(topo_y) == length(xv)\n\nBoth refresh the cached vertex topography and the previous-step buffers so the chain is ready to advect.","title":"Creating a chain"},{"category":"section","location":"marker_chain/#Advecting-a-chain","page":"Marker chain","text":"JustPIC offers two schemes to evolve a chain in a velocity field V (a tuple of the velocity component arrays) defined on the staggered grids grid_vxi.\n\nLagrangian — move the markers themselves, then reassign, resample, and rebuild the topography:\n\nadvect_markerchain!(chain, method, V, grid_vxi, dt)\n\nSemi-Lagrangian — solve for new vertex heights whose backward trajectories land on the old surface, smooth steep slopes, conserve the spatial mean height, and reconstruct the markers:\n\nsemilagrangian_advection_markerchain!(chain, method, V, grid_vxi, xvi, dt;\n    max_slope_angle = 45.0, conserve_mean = true)\n\nwhere xvi = (xv, yv) is the chain's vertex grid, with xv matching chain.cell_vertices. The surface must remain single-valued. Both wrappers conserve the mean height by default, using cell-width weights on refined grids. method is the time integrator: advect_markerchain! accepts Euler, RungeKutta2, or RungeKutta4, while the semi-Lagrangian scheme requires a backtracking-capable integrator (RungeKutta2 or RungeKutta4).\n\nSet conserve_mean = false for net uplift, subsidence, or boundary flux: restoring the old mean would remove that physical change. Set max_slope_angle = nothing or 90 to disable smoothing. Angles must be in [0, 90] degrees. Otherwise, one smoothing pass acts on slopes exceeding that angle; it does not guarantee that every slope ends below that angle. Straight lines are preserved even on refined grids.\n\nThe semi-Lagrangian solve enforces y_departure = h_old(x_departure), rather than backtracking from the old height and applying a single correction. The velocity is held fixed during each step. RK2/RK4 determine the trajectory integration order; the old topography is interpolated piecewise linearly, which also limits the overall accuracy. Departures outside the horizontal domain use constant endpoint heights, while velocities are extrapolated from their edge cells. These boundary choices are not periodic inflow conditions. A failed characteristic solve raises an error before changing the chain; reduce the timestep and check whether the surface is still single-valued.\n\nThe lower-level semilagrangian_advection!(chain, method, V, grid_vxi, xvi, dt) only updates h_vertices. It leaves marker coordinates and previous-step buffers unchanged, and applies neither smoothing nor mean-height correction.","title":"Advecting a chain"},{"category":"section","location":"marker_chain/#Resampling-and-topography","page":"Marker chain","text":"As a chain deforms, columns can become depleted or unevenly spaced. resample! refills any column that drops below min_xcell markers (or becomes too distorted) and restores a regular spacing:\n\nresample!(chain)\n\nadvect_markerchain! calls this internally, so you only need it when composing the advection steps yourself. To synchronize the cached vertex topography with the current marker positions use compute_topography_vertex!(chain).","title":"Resampling and topography"},{"category":"section","location":"marker_chain/#Coupling-to-a-two-phase-Stokes-solver","page":"Marker chain","text":"To use a marker chain as a free surface / air-rock interface, compute the fraction of each control volume that lies below the chain with compute_rock_fraction!:\n\ncompute_rock_fraction!(ratios, chain, xvi, dxi)\n\nratios is a container with center, vertex, Vx, and Vy fields (e.g. a PhaseRatios or a named tuple of arrays) sized to the respective grid locations; each is filled with a value in [0, 1]. xvi is the vertex grid and dxi the matching spacing: one Number per direction on a uniform grid, or one vector of per-cell widths per direction on a refined one.\n\nYou can also interpolate the grid velocity onto the current marker positions (for diagnostics or custom advection) with interpolate_velocity_to_markerchain!(chain, chain_V, V, grid_vxi), where chain_V is preallocated with the chain's cell layout.","title":"Coupling to a two-phase Stokes solver"},{"category":"section","location":"marker_chain/#Example","page":"Marker chain","text":"using JustPIC\nusing GLMakie\n\nconst backend = JustPIC.CPU\n\nfunction expand_range(x::AbstractRange)\n    dx = x[2] - x[1]\n    n = length(x)\n    x1, x2 = extrema(x)\n    xI = round(x1 - dx; sigdigits = 5)\n    xF = round(x2 + dx; sigdigits = 5)\n    return LinRange(xI, xF, n + 2)\nend\n\n# velocity field\nvi_stream(x) = π * 1e-5 * (x - 0.5)\n\n# Initialize domain & grids\nn = 51\nLx = Ly = 1.0\n# nodal vertices\nxvi = xv, yv = LinRange(0, Lx, n), LinRange(0, Ly, n)\ndxi = dx, dy = xv[2] - xv[1], yv[2] - yv[1]\n# nodal centers\nxc, yc = LinRange(0 + dx / 2, Lx - dx / 2, n - 1), LinRange(0 + dy / 2, Ly - dy / 2, n - 1)\n# staggered grid velocity nodal locations\ngrid_vx = xv, expand_range(yc)\ngrid_vy = expand_range(xc), yv\ngrid_vxi = grid_vx, grid_vy\n\n# Velocity defined on the grid\nVx = TA(backend)([-vi_stream(y) for x in grid_vx[1], y in grid_vx[2]])\nVy = TA(backend)([ vi_stream(x) for x in grid_vy[1], y in grid_vy[2]])\nV = Vx, Vy\n\n# Initialize marker chain\nnxcell, min_xcell, max_xcell = 12, 6, 24\ninitial_elevation = Ly / 2\nchain = init_markerchain(backend, nxcell, min_xcell, max_xcell, xv, initial_elevation)\nmethod = RungeKutta2()\n\n# time stepping\ndt = 200.0\nfor _ in 1:25\n    advect_markerchain!(chain, method, V, grid_vxi, dt)\nend\n\n# plot the chain\nf = Figure()\nax = Axis(f[1, 1])\npoly!(ax, Rect(0, 0, 1, 1), color = :lightgray)\npx = chain.coords[1].data[:]\npy = chain.coords[2].data[:]\nscatter!(px, py, color = :black)\ndisplay(f)","title":"Example"},{"category":"section","location":"marker_chain/#Refined-grid","page":"Marker chain","text":"The same example runs on a graded horizontal grid — only the construction of xv/yv changes. A non-range grid is indexed directly inside the kernels, so on a GPU backend it has to be moved to the device with TA(backend):\n\n# cell widths growing left to right by a factor of 4\nfunction graded(n, L)\n    widths = [1 + 3 * (i - 1) / (n - 2) for i in 1:(n - 1)]\n    x = cumsum(vcat(0.0, widths))\n    return L .* x ./ last(x)\nend\n\nexpand_vector(x) = vcat(x[1] - (x[2] - x[1]), x, x[end] + (x[end] - x[end - 1]))\n\nxv, yv = graded(n, Lx), graded(n, Ly)\nxc = (xv[1:(end - 1)] .+ xv[2:end]) ./ 2\nyc = (yv[1:(end - 1)] .+ yv[2:end]) ./ 2\n\ngrid_vx = TA(backend)(xv), TA(backend)(expand_vector(yc))\ngrid_vy = TA(backend)(expand_vector(xc)), TA(backend)(yv)\ngrid_vxi = grid_vx, grid_vy\n\nchain = init_markerchain(backend, nxcell, min_xcell, max_xcell, TA(backend)(xv), initial_elevation)\n\nThe time-stepping loop is unchanged. Where a uniform grid passes a scalar dxi to compute_rock_fraction!, a refined one passes the per-cell widths: dxi = diff(xv), diff(yv).\n\nThe scripts/rotating_marker_chain*.jl files in the repository provide runnable Lagrangian and semi-Lagrangian variants of this example, each with a *_refined.jl sibling.","title":"Refined grid"},{"category":"section","location":"marker_chain/#API","page":"Marker chain","text":"","title":"API"},{"category":"type","location":"marker_chain/#JustPIC.MarkerChain","page":"Marker chain","text":"MarkerChain{Backend,N,I,T1,T2,T3,TV} <: AbstractParticles\n\nContainer for a 2D marker chain used to represent a free surface or topographic interface as a single-valued height field y = h(x).\n\nMarkers are bucketed into the columns of a 1D grid (cell_vertices) using the same CellArray layout as Particles: each column holds up to max_xcell marker slots, and a boolean occupancy mask marks which are live.\n\nFields\n\ncoords::NTuple{N,T1}: marker coordinates, one CellArray per dimension. In 2D coords[1] is x and coords[2] is y. Empty slots hold NaN.\ncoords0::NTuple{N,T1}: marker coordinates from the previous time step.\nh_vertices::T2: topography sampled at the grid vertices (current step).\nh_vertices0::T2: topography at the vertices from the previous step; used by advect_markerchain!/semilagrangian_advection_markerchain! to conserve the mean height.\ncell_vertices::TV: the horizontal grid xv that defines the columns. It must be finite and strictly increasing; the spacing may be non-uniform, in which case every operation resolves each column's width from its own pair of vertices.\nindex::T3: per-slot occupancy mask (true ⟺ the matching coords slot is live).\nmin_xcell, max_xcell::I: the minimum and maximum number of markers allowed per column; resample! refills depleted columns back up to min_xcell.\n\nInvariants\n\nA slot is live iff its mask entry is true; live slots have finite coordinates and empty slots are NaN.\nMarker precision follows eltype(cell_vertices)/the initial elevation, so a Float32 grid yields Float32 markers (needed on Metal, which has no Float64).\n\nUse init_markerchain to create a chain, fill_chain_from_chain! or fill_chain_from_vertices! to overwrite its geometry, and advect_markerchain! or semilagrangian_advection_markerchain! to evolve it in time.\n\n\n\n\n\n","title":"JustPIC.MarkerChain"},{"category":"function","location":"marker_chain/#JustPIC.cell_length","page":"Marker chain","text":"cell_length(chain::MarkerChain, i::Integer)\ncell_length(chain::MarkerChain)\n\nReturn the horizontal width of column i of a 2D marker chain, that is chain.cell_vertices[i + 1] - chain.cell_vertices[i].\n\nThe one-argument method returns the width shared by every column, and is therefore defined only when chain.cell_vertices is uniformly spaced; on a refined grid it throws an ArgumentError.\n\n\n\n\n\n","title":"JustPIC.cell_length"},{"category":"function","location":"marker_chain/#JustPIC.init_markerchain","page":"Marker chain","text":"init_markerchain(backend, nxcell, min_xcell, max_xcell, xv, initial_elevation)\n\nCreate a 2D MarkerChain sampled along the horizontal grid xv.\n\nThe vertices in xv must be finite and strictly increasing; the spacing may be non-uniform, in which case each column is populated according to its own width.\n\nnxcell controls the initial number of markers per cell, while initial_elevation can be either a scalar or a vector specifying the initial surface height.\n\nReturns\n\nA MarkerChain whose marker positions, vertex topography, and occupancy masks are initialized consistently.\n\n\n\n\n\n","title":"JustPIC.init_markerchain"},{"category":"function","location":"marker_chain/#JustPIC.fill_chain_from_chain!","page":"Marker chain","text":"fill_chain_from_chain!(chain::MarkerChain, topo_x, topo_y)\n\nReplace the marker positions in chain with coordinates sampled from an existing topographic polyline.\n\nAfter the markers are reassigned, the vertex-based topography stored on the chain is recomputed and synchronized with h_vertices0.\n\ntopo_x and topo_y should describe an open polyline that spans the chain's horizontal extent.\n\n\n\n\n\n","title":"JustPIC.fill_chain_from_chain!"},{"category":"function","location":"marker_chain/#JustPIC.fill_chain_from_vertices!","page":"Marker chain","text":"fill_chain_from_vertices!(chain::MarkerChain, topo_y)\n\nReconstruct a marker chain from topography values given at grid vertices.\n\ntopo_y is copied into both the current and previous vertex topography fields before the marker coordinates are rebuilt.\n\nThis is useful when the interface is naturally represented on the vertex grid and you want to refresh the marker representation from that discretization.\n\n\n\n\n\n","title":"JustPIC.fill_chain_from_vertices!"},{"category":"function","location":"marker_chain/#JustPIC.advect_markerchain!","page":"Marker chain","text":"advect_markerchain!(chain, method, V, grid_vxi, dt)\n\nAdvect a marker chain for one time step and rebuild its derived topography data.\n\nThis convenience wrapper runs marker advection, reassigns markers to cells, resamples the chain, updates vertex elevations, and enforces mean-height conservation.\n\nUse this when evolving a free surface or interface represented by a MarkerChain.\n\n\n\n\n\n","title":"JustPIC.advect_markerchain!"},{"category":"function","location":"marker_chain/#JustPIC.semilagrangian_advection_markerchain!","page":"Marker chain","text":"semilagrangian_advection_markerchain!(chain, method, V, grid_vxi, grid, dt; max_slope_angle = 45.0, conserve_mean = true)\n\nBacktrack a marker chain through V and update the chain geometry with a semi-Lagrangian step.\n\nUnlike advect_markerchain!, which moves the Lagrangian markers, this scheme solves for the new vertex heights whose backward trajectories land on the old surface. It then smooths slopes exceeding max_slope_angle degrees, restores the spatial mean height, and rebuilds the markers. Smoothing is a single pass, not a strict slope bound; use max_slope_angle = nothing (or 90 degrees) to disable it. Angles must be in [0, 90]. Set conserve_mean = false when the velocity field should change the mean height (for example, uniform uplift or boundary flux). Mean conservation uses cell-width weights, including on refined grids.\n\nmethod must support backtracking (RungeKutta2 or RungeKutta4; Euler is not supported). grid_vxi holds the staggered velocity grids and grid the chain's vertex grid (xv, yv), whose horizontal coordinates must match chain.cell_vertices. The surface must remain single-valued; reduce dt if the characteristic solve fails.\n\n\n\n\n\n","title":"JustPIC.semilagrangian_advection_markerchain!"},{"category":"function","location":"marker_chain/#JustPIC.resample!","page":"Marker chain","text":"resample!(chain::MarkerChain)\n\nResample the markers within each chain cell when the chain becomes too sparse or too distorted.\n\nThis keeps the marker spacing reasonably regular, which improves interpolation quality and the stability of subsequent marker-chain operations.\n\n\n\n\n\n","title":"JustPIC.resample!"},{"category":"function","location":"marker_chain/#JustPIC.compute_topography_vertex!","page":"Marker chain","text":"compute_topography_vertex!(chain::MarkerChain)\n\nInterpolate the marker-chain geometry back to the vertex-based topography array chain.h_vertices.\n\nThis is typically called after marker advection or resampling.\n\n\n\n\n\n","title":"JustPIC.compute_topography_vertex!"},{"category":"method","location":"marker_chain/#JustPIC.compute_rock_fraction!-Tuple{Any, MarkerChain, Any, Any}","page":"Marker chain","text":"compute_rock_fraction!(ratios, chain::MarkerChain, xvi, dxi)\n\nFill ratios with the fraction of each control volume that lies below the marker chain.\n\nThe result is written at cell centers, vertices, and staggered velocity nodes using the topography currently stored in chain.\n\nxvi are the cell vertices per direction and dxi the matching spacings: either one Number per direction for a uniform grid, or one AbstractVector of per-cell widths per direction for a refined one.\n\n\n\n\n\n","title":"JustPIC.compute_rock_fraction!"},{"category":"function","location":"marker_chain/#JustPIC.interpolate_velocity_to_markerchain!","page":"Marker chain","text":"interpolate_velocity_to_markerchain!(chain::MarkerChain, chain_V::NTuple{N, CellArray}, V, grid_vi::NTuple{N, NTuple{N, T}}) where {N, T}\n\nInterpolate the staggered velocity field V to the current marker positions in chain and store the result in chain_V.\n\nchain_V must be preallocated with the same cell layout as the marker-chain coordinates.\n\n\n\n\n\n","title":"JustPIC.interpolate_velocity_to_markerchain!"},{"category":"section","location":"marker_surface/#Marker-surface","page":"Marker surface","text":"In three-dimensional geodynamic modeling it is often useful to track a free surface (e.g. the topographic interface between rock and a sticky-air layer). JustPIC.jl provides the MarkerSurface object for this purpose: a structured 2D height field topo[i,j] defined on a regular horizontal vertex mesh (xv[i], yv[j]), living on top of the 3D staggered grid.\n\nWe can instantiate a marker surface at a constant elevation h as:\n\nsurf = init_marker_surface(backend, xv, yv, h)\n\nwhere backend is the device backend, xv and yv are finite, strictly increasing 1D arrays/ranges of the horizontal grid vertices (lengths nx+1 and ny+1), and h is either a scalar elevation or an (nx+1)×(ny+1) array of initial heights. Optional keyword arguments periodic_1 and periodic_2 enable periodic boundary conditions in the first / second horizontal direction.\n\nWe can also overwrite the topography of an existing surface from a 2D array:\n\n# set topography from a pre-computed height field\nz_init = [0.4 + 0.1 * sin(2π * xv[i]) * cos(2π * yv[j])\n          for i in 1:length(xv), j in 1:length(yv)]\nset_topo_from_array!(surf, z_init)\n\nThe surface is then advected one time step with:\n\nadvect_marker_surface!(surf, V, grid_vxi, dt)\n\nwhere V = (Vx, Vy, Vz) is a tuple of 3D velocity arrays and grid_vxi = (grid_vx, grid_vy, grid_vz) gives one (x, y, z) coordinate tuple per staggered component. Each velocity-array size must equal the lengths of its own coordinate tuple. dt is the time step. The driver interpolates the staggered velocity field onto surface nodes, advects the height field with a deformed-grid triangle scheme, and optionally smooths slopes exceeding max_slope_angle (default 45°).\n\nCoordinates may be ranges or vectors, and both are fully supported. A uniformly spaced grid passed as a range lets the interpolation locate the containing cell by arithmetic; the same grid passed as a vector costs a binary search per surface node instead, roughly tripling the interpolation time. Non-uniformly spaced grids — adaptively refined meshes, for instance — are vectors by nature and take the search path.\n\nFor coupling with a Stokes solver, the volumetric fraction of each cell that lies below the free surface (the \"rock fraction\") at every staggered-grid position is computed by:\n\ncompute_rock_fraction!(ratios, surf, xvi, dxi)\n\nratios is a named tuple of arrays sized at cell centres, vertices, faces, and edges. The function evaluates the geometric fraction of every corresponding control volume from a piecewise-planar surface, splitting each cell into eight triangles that meet at the cell center.","title":"Marker surface"},{"category":"section","location":"marker_surface/#Examples","page":"Marker surface","text":"Run the complete CPU examples, which construct the component-specific staggered grids and regenerate the figures below:\n\njulia --project=docs docs/examples/marker_surface.jl\n\nThe batch example uses CairoMakie, colouring synthetic surfaces with Makie's :oleron colormap and Matterhorn elevations with :grayC10. GLMakie 0.13 remains available in the docs environment for interactive rendering.\n\nThe nonperiodic figures use a 193×193, approximately 8 m sample centered on the Matterhorn summit (45.9763° N, 7.6586° E). It combines swisstopo's 0.5 m swissALTI3D DEM on the Swiss side with ARPA Piemonte's transboundary terrain service on the Italian side, whose Valle d'Aosta input is a 2 m LiDAR DTM. Both sources are resampled to the example grid, preserving the distinct cross-border summit kink. The simulation rescales elevations to its unit-height domain, but the Matterhorn figures map them back to the original DEM elevation range and show local horizontal coordinates in meters. The periodic figures retain a synthetic surface: an isolated mountain is not a periodic terrain field.\n\n(Image: Initial nonperiodic surface)\n\n(Image: Advected nonperiodic surface)","title":"Examples"},{"category":"section","location":"marker_surface/#Periodic-boundary-conditions","page":"Marker surface","text":"Pass periodic_1 = true and/or periodic_2 = true to init_marker_surface to enable wrap-around boundaries in the first (x) or second (y) horizontal direction. The flags are stored on the surface object and read automatically by every advection and smoothing call — no need to forward them explicitly.\n\nsurf = init_marker_surface(backend, xv, yv, 0.5;\n                           periodic_1 = true,   # x periodic\n                           periodic_2 = false)  # y non-periodic\n\nUnder periodic boundaries the ghost cells used by the advection stencil wrap to the opposite side instead of being linearly extrapolated, the slope-limiter's neighbour lookups use mod1 indexing, and the redundant boundary nodes (topo[1, :] and topo[end, :]) are kept synchronised after every update. This follows the same convention used by move_particles!(…; periodic_1, periodic_2, periodic_3) in JustPIC's particle advection.\n\nThe periodic figures below translate a Gaussian bump with a uniform Vx = 0.2 for 25 steps of dt = 0.05. Centered at x = 0.9 to begin with, it crosses the right boundary and re-enters on the left, ending near x = 0.16 — the rise at both x edges of either figure is the same bump seen across the seam. scripts/marker_surface_advection_periodic.jl runs the same setup with a live GLMakie view.\n\n(Image: Initial periodic surface)\n\n(Image: Advected periodic surface)","title":"Periodic boundary conditions"},{"category":"section","location":"marker_surface/#Multi-GPU-/-MPI-(ImplicitGlobalGrid)","page":"Marker surface","text":"The marker surface works with ImplicitGlobalGrid.jl domain decomposition, including full 3D (x, y and z) decomposition. Build the surface from the local (rank) vertex coordinates; when a global grid is active, advect_marker_surface! and maximum-angle smoothing automatically exchange the topography's x/y halo between neighbouring ranks (ImplicitGlobalGrid.update_halo!). When the grid is decomposed in z, the surface velocity is additionally combined across each z-column so every node is interpolated from the rank whose slab contains the surface elevation — no user action required.\n\nme, dims, nprocs = init_global_grid(nx, ny, nz; periodx = 1, periody = 1)\n\n# local vertex coordinates of this rank\nxv = [x_g(i, dx, Vx) for i in 1:(nx + 1)]\nyv = [y_g(j, dy, Vy) for j in 1:(ny + 1)]\n\nsurf = init_marker_surface(backend, xv, yv, initial_elevation)\n\nfor _ in 1:nt\n    advect_marker_surface!(surf, V, grid_vxi, dt)   # halo exchange included\nend\n\nNotes:\n\nUnder MPI, set periodicity through init_global_grid (periodx/periody) and leave periodic_1/periodic_2 of the surface as false — the local flags wrap within the rank-local array and are only meant for single-device runs.\nIf you modify surf.topo manually, call update_surface_halo!(surf) afterwards to synchronize the ranks.\ncompute_avg_topo performs an owned-node global reduction, excluding overlapping x/y nodes and duplicate z-column replicas.","title":"Multi-GPU / MPI (ImplicitGlobalGrid)"},{"category":"section","location":"marker_surface/#Limitations","page":"Marker surface","text":"Topography, surface velocities, and horizontal coordinates use the promoted floating-point type of xv, yv, and the initial elevation.\nUnder z-decomposition, interpolate_velocity_to_surface_vertices! performs a few small Allreduces per call over each z-column, directly on the device velocity arrays (no host staging). On GPU this requires GPU-aware MPI — the same IGG_CUDAAWARE_MPI=1 / IGG_ROCMAWARE_MPI=1 setup ImplicitGlobalGrid already uses for its halo exchange. Grids decomposed only in x/y have no such requirement.","title":"Limitations"},{"category":"section","location":"marker_surface/#API","page":"Marker surface","text":"","title":"API"},{"category":"type","location":"marker_surface/#JustPIC.MarkerSurface","page":"Marker surface","text":"MarkerSurface{Backend, T2, TV, TB, TW} <: AbstractParticles\n\nA 3D free surface tracker using a structured marker grid. The surface is represented as a 2D grid of topography values (z-heights) at corner nodes.\n\nFields\n\ntopo::T2       — topography (z-elevation) at grid vertices, size (nx+1, ny+1)\ntopo0::T2      — topography from the previous time step\nvx::T2         — x-velocity interpolated to surface nodes\nvy::T2         — y-velocity interpolated to surface nodes\nvz::T2         — z-velocity interpolated to surface nodes\nxv::TV         — x-coordinates of surface grid vertices\nyv::TV         — y-coordinates of surface grid vertices\nperiodic_1::Bool — periodic boundary in x\nperiodic_2::Bool — periodic boundary in y\nadvection_valid::TB — persistent validity mask for topography advection\nsmoothing_cell_topo::TW — persistent cell-centered smoothing workspace\nsmoothing_steep::TB — persistent steep-cell mask\nz_ownership::TW — persistent z-column ownership weights\n\n\n\n\n\n","title":"JustPIC.MarkerSurface"},{"category":"function","location":"marker_surface/#JustPIC.init_marker_surface","page":"Marker surface","text":"init_marker_surface(::Type{backend}, xv, yv, initial_elevation;\n                    periodic_1=false, periodic_2=false)\n\nCreate a MarkerSurface that tracks a 3D free surface on the grid defined by vertex coordinates xv and yv.\n\nThe topography is stored at the grid vertices (corner nodes), matching LaMEM's FreeSurf approach where the surface DMDA has the same (x,y)-resolution as the staggered-grid corner nodes.\n\nArguments\n\nbackend : KernelAbstractions backend type such as CPU, CUDA, AMDGPU, Metal\nxv      : 1D array/range of x-coordinates of grid vertices (length nx+1)\nyv      : 1D array/range of y-coordinates of grid vertices (length ny+1)\ninitial_elevation : scalar or 2D array (nx+1)×(ny+1) of initial z-elevations\nperiodic_1, periodic_2 : periodic boundary conditions in x and y (default false)\n\nReturns\n\nA MarkerSurface instance with topography initialised to initial_elevation.\n\n\n\n\n\n","title":"JustPIC.init_marker_surface"},{"category":"function","location":"marker_surface/#JustPIC.set_topo_from_array!","page":"Marker surface","text":"set_topo_from_array!(surf::MarkerSurface, z::AbstractMatrix)\n\nSet the surface topography from a 2D array z of size (nx+1, ny+1). Also copies the values into topo0.\n\n\n\n\n\n","title":"JustPIC.set_topo_from_array!"},{"category":"function","location":"marker_surface/#JustPIC.compute_avg_topo","page":"Marker surface","text":"compute_avg_topo(surf::MarkerSurface)\n\nCompute and return the average topography over all surface vertices. Duplicated periodic seam nodes are counted once. Under MPI, overlapping x/y nodes and replicated z-columns are counted once through an owned-node global reduction. Note: forces a device→host scalar transfer on GPU; call only outside hot loops.\n\n\n\n\n\n","title":"JustPIC.compute_avg_topo"},{"category":"function","location":"marker_surface/#JustPIC.interpolate_velocity_to_surface_vertices!","page":"Marker surface","text":"interpolate_velocity_to_surface_vertices!(surf::MarkerSurface, V, grid_vxi)\n\nInterpolate the 3D velocity field V = (Vx, Vy, Vz) onto the free surface nodes. Each surface node at position (xv[i], yv[j], topo[i,j]) receives trilinearly interpolated velocity values.\n\nArguments\n\nsurf : the MarkerSurface\nV    : tuple (Vx, Vy, Vz) of 3D velocity arrays\ngrid_vxi : tuple of component grids (grid_vx, grid_vy, grid_vz), where each component grid is its own (x, y, z) coordinate tuple\n\n\n\n\n\n","title":"JustPIC.interpolate_velocity_to_surface_vertices!"},{"category":"function","location":"marker_surface/#JustPIC.advect_surface_topo!","page":"Marker surface","text":" advect_surface_topo!(surf::MarkerSurface, dt)\n\nAdvect the topography on the free surface mesh using the velocity field already interpolated onto the surface nodes (surf.vx, surf.vy, surf.vz).\n\nBuild ghost coordinates and field values outside the domain: periodic images  of the wrapped nodes when periodic, otherwise extrapolated coordinates with  field values clamped to the boundary node.\nFor each surface node, build a local 3×3 \"deformed grid\" using neighboring  node positions displaced by dt*v.\nSubdivide the deformed cell into 16 triangles (9 corner + 4 midpoint nodes).\nFind which triangle contains the target position and perform barycentric interpolation of the z-coordinate.\n\n    1 ------- 2 ------- 3\n    |  \\     / \\     /  |\n    |   \\   /   \\   /   |\n    |    \\ /     \\ /    |\n    |    10       11    |\n    |    / \\     / \\    |\n    |   /   \\   /   \\   |\n    |  /     \\ /     \\  |\n    4 ------- 5 ------- 6\n    |  \\     / \\     /  |\n    |   \\   /   \\   /   |\n    |    \\ /     \\ /    |\n    |    12       13    |\n    |    / \\     / \\    |\n    |   /   \\   /   \\   |\n    |  /     \\ /     \\  |\n    7 ------- 8 ------- 9\n\nArguments\n\nsurf : the MarkerSurface\ndt   : time step\n\n\n\n\n\n","title":"JustPIC.advect_surface_topo!"},{"category":"function","location":"marker_surface/#JustPIC.advect_marker_surface!","page":"Marker surface","text":"advect_marker_surface!(surf::MarkerSurface, V, grid_vxi, dt;\n                       max_slope_angle=45)\n\nMain driver to advect the free surface:\n\nInterpolate velocities from the 3D grid to surface nodes\nAdvect topography using the deformed-grid triangle method\nSmooth topography spikes (if max_slope_angle > 0)\n\nArguments\n\nsurf : the MarkerSurface\nV    : tuple (Vx, Vy, Vz) of 3D velocity arrays\ngrid_vxi : tuple of component grids (grid_vx, grid_vy, grid_vz)\ndt   : time step\nmax_slope_angle : maximum slope angle in degrees (default 45; ≤ 0 disables smoothing)\n\n\n\n\n\n","title":"JustPIC.advect_marker_surface!"},{"category":"function","location":"marker_surface/#JustPIC.smooth_surface_max_angle!","page":"Marker surface","text":"smooth_surface_max_angle!(surf::MarkerSurface, max_slope_angle)\n\nSmooth the topography where the slope angle exceeds max_slope_angle (in degrees, matching the MarkerChain smooth_slopes! convention).\n\nThis mirrors LaMEM's FreeSurfSmoothMaxAngle:\n\nScan all cells, compute the max slope (tan) from the 4 corner nodes and the average cell height; mark cells exceeding tan(max_angle).\nFor each node touching at least one marked cell, replace its topography with the average of the (up to 4) surrounding cell-center heights.\n\nArguments\n\nsurf            : the MarkerSurface\nmax_slope_angle : maximum slope angle in degrees (e.g. 45.0)\n\n\n\n\n\n","title":"JustPIC.smooth_surface_max_angle!"},{"category":"function","location":"marker_surface/#JustPIC.update_surface_halo!","page":"Marker surface","text":"update_surface_halo!(surf::MarkerSurface)\n\nExchange the x/y MPI halo of surf.topo between neighbouring ranks of the active ImplicitGlobalGrid global grid. No-op when no global grid is initialized (serial runs).\n\nCalled automatically at the end of advect_surface_topo!, smooth_surface_max_angle!; only needed explicitly after modifying surf.topo by hand.\n\nNotes\n\nsurf must be built from the local (rank) vertex coordinates.\nUnder MPI, use the global-grid periodicity (periodx/periody in init_global_grid) and leave surf.periodic_1/periodic_2 as false; the local periodic flags wrap within the rank-local array.\n\n\n\n\n\n","title":"JustPIC.update_surface_halo!"},{"category":"method","location":"marker_surface/#JustPIC.compute_rock_fraction!-Tuple{Any, MarkerSurface, Any, Any}","page":"Marker surface","text":"compute_rock_fraction!(ratios, surf::MarkerSurface, xvi, dxi)\n\nCompute the rock fraction (fraction of each cell volume below the free surface) at all staggered-grid positions and store them in ratios.\n\nThis is the 3D equivalent of compute_rock_fraction!(ratios, chain::MarkerChain, xvi, dxi). The ratios struct must have fields .center, .vertex, .Vx, .Vy, .Vz, .xy, .yz, and .xz.\n\nArguments\n\nratios : struct with center, vertex, face, and edge arrays\nsurf   : the MarkerSurface\nxvi    : tuple (xv, yv, zv) of 1D vertex coordinate arrays\ndxi    : tuple (dx, dy, dz) of grid spacings (kept for API consistency            with the 2D version; the 3D kernel uses xvi directly)\n\n\n\n\n\n","title":"JustPIC.compute_rock_fraction!"}]
}
